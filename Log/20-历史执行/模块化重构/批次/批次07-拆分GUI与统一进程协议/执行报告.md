# 批次 07 执行报告

## 结论

批次 7 已完成。GUI 已从单文件拆为窗口、控制器、进程、设置和 widgets，QProcess 统一调用 `python -m whisper_subtitle transcribe`，不再按 preset 的 Core 脚本或模块名分派。普通 CLI 文本输出保持兼容，同时新增 JSON Lines 机器进度模式。

本批次未修改转录算法、preset 参数或输出规则，未执行 Git 暂存、提交、标签或推送，`.workbuddy` 未改动。

## 主要调整

### 统一进程协议

- `cli.py` 的 `transcribe` 直接构造 `TranscriptionRequest` 并调用批次 6 的统一应用服务。
- 新增 `--progress text|jsonl`；默认仍为 `text`。
- JSONL 每行包含 `type/stage/message/current/total/preset_id/input_path`。
- GUI 命令固定为统一入口，并显式传递 preset CLI alias、输出目录、desktop 和 JSONL 模式。
- JSONL 解析对第三方非 JSON 输出保持容错，原样转入日志。

### GUI 分层

- `presentation/gui/main_window.py`：窗口生命周期、文件对话框、日志呈现和交互协调。
- `presentation/gui/controller.py`：运行态、用户停止语义、进度与完成事件。
- `presentation/gui/process_runner.py`：QProcess、UTF-8 环境、行缓冲、JSONL 解码、terminate 与 5 秒超时 kill。
- `presentation/gui/settings.py`：QSettings 的路径、preset、geometry 读写和旧值归一化。
- `presentation/gui/widgets/panels.py`：设置、参数/监控、日志三个原样式面板。
- `presentation/gui/widgets/performance.py`：性能折线图。
- 历史 `gui/WhisperPyQtGUI.py` 缩为兼容门面，保留旧导入路径和 `main()`。

原 `_build_ui()` 从 509 行降为 2 行委派；三个面板构建方法分别为 195、144、147 行。presentation GUI 静态扫描中没有 `WhisperProject`、`preset.module` 或 `preset.script`。

## 行为保留

- 进程隔离仍使用 QProcess，没有改为 GUI 线程内加载 GPU 模型。
- 停止仍先异步 `terminate()`，5 秒后仍运行才 `kill()`。
- 关闭运行中的窗口仍先忽略 close event、请求停止，收到完成事件后再关闭。
- QSettings 键、QFileDialog 起始目录、窗口 geometry、preset 恢复和 desktop 选项保持。
- 原界面配色、尺寸、三栏布局、参数表、性能图表和日志样式保持。
- 四个历史 Core CLI 入口仍可用。

## 验证结果

- 最终全量测试：`230 passed in 0.62s`。
- 源码编译：41 个 Python 文件通过。
- GUI 交互测试覆盖启动、文件选择、preset/参数切换、停止、强制终止、关闭等待和设置恢复。
- 真实 QProcess 对无效输入成功解析 `input_invalid` JSONL，退出码为 1。
- 普通文本和 JSONL 无效输入均为退出码 1；六个 CLI/Core help 入口均为 0。
- 模块 GUI与历史兼容 GUI 在 offscreen 环境均稳定存活 3 秒。
- `git diff --check` 通过。

## 性能与黄金结果

24/24 次基准成功，所有 preset 的 `preset_id/script/model/params/postprocess/output` 与基线相同。

| preset | cold 比例 | warm 比例 |
| --- | ---: | ---: |
| cn | 1.003 | 0.989 |
| cn2 | 0.985 | 0.996 |
| en | 0.983 | 0.997 |
| en2 | 0.983 | 0.993 |

最大耗时中位数比例为 `1.003`，低于 `1.20` 阈值。输出 SHA256 未变：

- English：`D6AD4418CE220D3981878CB84FB903B6E483FF2497FCBD6A9D5C65C15463DE78`
- Chinese：`69D77F85DDD53B7F98F9A0E63F3639D6F8D194F33430278B96CBAAE798E2CD3D`

## 仓库边界

- Git 暂存区：0 个文件。
- `.workbuddy`：0 个变化。
- 未执行自动提交。
- 批次基准输出、pytest/Python 缓存已清理。
- GUI、转录和 benchmark Python 遗留进程：0。
- 用户既有 `.claude/settings.local.json`、`Requirement/isolate.md` 及批次 0–6 累积改动均保留。
