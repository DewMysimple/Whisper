# 新架构批次 0 执行报告

状态：`Complete / Accepted`。

执行日期：2026-07-15。

## 1. 批次目标

冻结迁移前功能和界面证据，形成正式架构决策、产品规则、逻辑仓库边界、版本策略和打包方向；不创建新架构运行时代码。

## 2. 本批产物

- `docs/architecture/adr/0001-tauri-react-python-worker.md`
- `Log/20-第二阶段-Tauri桌面架构迁移/批次0-架构决策与产品基线/功能与交互基线.md`
- `Log/20-第二阶段-Tauri桌面架构迁移/批次0-架构决策与产品基线/产品与技术决策记录.md`
- `Log/20-第二阶段-Tauri桌面架构迁移/批次0-架构决策与产品基线/界面截图/current-main-parameters.png`
- `Log/20-第二阶段-Tauri桌面架构迁移/批次0-架构决策与产品基线/界面截图/current-main-monitoring.png`
- 本执行报告。

## 3. 已完成内容

- 记录当前 PyQt5 参数页和监视页截图，并保存 SHA-256。
- 形成当前输入、输出、preset、参数、日志、性能、设置、停止和关闭流程基线。
- 形成当前错误与恢复路径清单。
- 正式记录 Tauri 2 + React/TypeScript + Python Worker ADR。
- 确认本地 EXE/WebView2/受控 IPC/Python 核心/PyQt5 回退边界。
- 把带引号路径、多输入、派生自定义模式和参数实时交互写成产品规则。
- 给出 TXT/Markdown 输出模型、参数白名单、Windows/WebView2 和工具链推荐。
- 给出 Worker onedir、NSIS 安装版和便携目录版推荐布局。
- 明确逻辑 monorepo 边界，但未创建 `apps/`、`contracts/` 或 `packaging/`。

## 4. 验证结果

| 验证 | 结果 |
| --- | --- |
| Python 全量测试 | `176 passed in 2.99s` |
| 环境自检 | `环境自检通过` |
| `pip check` | `No broken requirements found.` |
| CLI 冒烟 | `python -m whisper_subtitle --help` 正常列出 `transcribe/gui/check` |
| 参数页截图 | 1512 × 882，目视检查通过，SHA-256 已记录 |
| 监视页截图 | 1512 × 882，强制整窗重绘后目视检查通过，SHA-256 已记录 |
| 用户设置隔离 | 截图使用内存设置仓库，未读写用户 QSettings |
| 生产代码/依赖/golden | 无差异 |

本批只变更文档和截图，没有变更 CUDA、依赖、推理或打包，因此没有重复安装环境或执行真实 GPU 推理。真实 CUDA、四 preset golden 和安装验证继续引用模块化基线的既有证据；后续凡涉及 Worker、依赖或打包，必须重新执行对应真实验证。

## 5. 明确未执行

- 未修改 `src/` 生产代码。
- 未修改测试、golden、preset、后处理、文件命名或输出内容。
- 未创建 Tauri、React、TypeScript、Rust 或 Worker 工程。
- 未创建 `apps/`、`contracts/`、`packaging/`。
- 未安装 Node.js、pnpm、Rust、Tauri、前端或 Python 依赖。
- 未执行真实转录、CUDA 推理或打包。
- 未修改、删除或移动 `.workbuddy`、Agent 本地配置和用户现有变化。
- 未恢复或创建 `.planning`。
- 未执行 Git 暂存、提交、创建分支、推送、标签或发布。

## 6. 用户验收结论

用户于 2026-07-15 验收批次 0，并确认：

1. TXT/Markdown 采用“共享根目录 + 可选独立目录覆盖”。
2. Markdown 默认关闭，以保持当前行为。
3. 接受首批参数白名单和建议安全范围。
4. 正式支持范围仅为 Windows 11 x64；Windows 10 22H2 x64 不做兼容验证。
5. 接受 Evergreen WebView2 + NSIS `offlineInstaller`。
6. Python Worker 首选 PyInstaller `onedir`。

批次 0 已关闭。批次 1 仍需用户明确提示后才能开始。
