# UI 快照：UI-2026-07-18-R02

冻结日期：2026-07-18。

快照类型：正式 React UI 反向抽离后的跨阶段调和基线。

## 形成原因

第一次第三阶段到第四阶段的回迁后，两边继续存在页面结构、可见文字、时间语义和 CSS 级联差异。用户明确要求先把正式性能页修成真实 `15s / 7.5s / 现在`，再“以目前的软件 UI 层为基准，让 HTML 同步”。因此本快照的方向不同于 R01：R01 是第三阶段设计输入，R02 是正式 UI 与第三阶段 HTML 重新对齐后的共同起点。

本次反向同步由用户明确授权，不建立自动同步机制。R02 冻结后，第三阶段仍可独立修改；正式工程只在用户再次明确授权时采用后续差异。

## 来源与代码标识

- 正式 UI：`F:\WhisperSubtitle\apps\web\src`。
- 第三阶段：`C:\Users\Administrator\Desktop\WhisperUI`。
- 第三阶段 Git 分支/HEAD：`main` / `2b6b716 chore: initialize WhisperUI repository`。
- 两边工作区均包含用户既有未提交变化，因此 Git HEAD 不能单独代表快照。
- 正式 `apps/web/src/styles.css` 与第三阶段 `src/styles/formal-ui-baseline.css` SHA-256：`DCA318DCB53A277B978570FE683ACB2777AB827394FB9068A2CC158B0FBA2D23`，逐字节一致。
- 第三阶段当前加载顺序：`formal-ui-baseline.css` → `prototype-adapter.css`。
- 第三阶段根预览与 `dist/index.html` SHA-256：`52BA1DA918721C99456E3FD01E1A5C468909F477C6E0D434CF1E52B21743F5C0`，内容一致。
- 第三阶段代码/文档清单摘要：`3B8D0F648F2D03869EB72153ABC60B0699742EF8D79F62CA542E88C451E2EF53`。
- 清单算法：覆盖根 `README.md`、`build-preview.ps1`、两份生成预览、`src` 和 `Log` 全部文件；按 `/` 分隔的相对路径排序，将 `相对路径|字节数|文件SHA-256` 以 LF 连接后计算 SHA-256，共 48 个文件。

## 页面调和范围

| 第三阶段页面/结构 | 正式 React 对应 | R02 状态 |
| --- | --- | --- |
| `shell.html`、`sidebar.html`、`topbar.html` | `App.tsx`、`Sidebar.tsx`、`LaunchCard.tsx` | 外壳、主页状态、导航、健康状态和错误提示已对齐 |
| `pages/home.html` | `PersonalHome.tsx` | 图片资产、身份、联系方式、入口和箭头已对齐 |
| `pages/workspace.html` | `SourcePanel.tsx`、`PresetPanel.tsx`、`OutputPanel.tsx`、工作台组合 | 输入、Preset、参数、输出、媒体队列和启动区已对齐；初始队列为空 |
| `pages/tasks.html`、`task-detail.html` | `TaskList.tsx` | 四项摘要、五种筛选、任务行、空状态和详情已对齐 |
| `pages/performance.html` | `PerformanceStrip.tsx` | 80 × 20、真实语义 `15s / 7.5s / 现在`、链路和详情字段已对齐 |
| `pages/settings.html` | `SettingsView.tsx` | 主题、运行环境、便携配置和 Worker 日志结构已对齐 |

## 演示边界

R02 只同步 UI 结构和视觉。第三阶段仍不连接 Tauri、Desktop IPC 或 Worker；性能、任务、模型、运行环境、路径、日志和媒体都是明确的演示状态。正式软件中的真实文件选择、任务执行、配置读写、GPU 采样、Preset 语义及输出行为没有被复制进 HTML。

## 验收标识

- 第三阶段构建：成功；两份预览哈希一致。
- 第三阶段本地 Chrome DOM/计算样式检查：0 控制台错误；五个页面可达；性能网格 80 × 20 且横轴精确；任务详情可打开；工作台初始队列为空。
- 正式 React：格式、Lint、类型和 12 条单元测试通过；4 条 Playwright E2E 通过。
- 正式 Tauri Release：构建成功；桌面 Host 与 `release/worker/whisper-subtitle-worker.exe` 子进程持续运行并响应。

详细执行见[第 3 次修改](后续UI修改日志/第3次修改.md)。
