# 功能测试 009：剪贴板输入与多媒体进度监控

测试日期：2026-07-24。  
状态：自动化、视觉检查、真实 CUDA 双媒体、正式 Desktop 和启动烟测通过。

## 测试目标

验证第 23 次修改的四条功能链：

1. Windows 剪贴板路径无需展开输入框即可进入媒体队列；
2. 输入区、SRT 禁用参数和媒体队列在宽窄窗口及字号变化下保持可用；
3. 多媒体任务整体进度单调不减，任务汇总正确完成；
4. 任务监控和性能监控显示简洁且真实的任务元数据。

## 自动化覆盖

### 剪贴板

- 单条未加引号路径及路径内空格；
- Explorer 多条双引号路径；
- 同一行多个双引号路径；
- UNC 路径；
- 大小写重复路径去重；
- 任意文本不作为路径；
- Tauri 原生 `readText` 桥接；
- Host 对存在、无效和目录路径继续执行真实检查。

### 进度与监控

- 多媒体跨阶段不回退；
- 稀疏、晚到和失败事件；
- 99% 汇总与 100% 终态；
- 单文件、多文件、文件夹和混合来源摘要；
- 性能任务卡日期、Preset 和模型；
- 任务汇总完成勾选。

### UI 与桌面

- 输入区域宽屏约 7:3，窄屏自然排列；
- SRT 模式文本参数可见、暗淡且不可交互；
- 文本 Preset 仍可切回 TXT/MD；
- 启动/队列卡与参数卡等高，队列内部滚动；
- “无操作 / 关机 / 休眠”均显示；
- 浅色、深色、主题强调色、字号、键盘焦点和减少动态效果回归。

结果：

- Python 294 项通过；
- Rust 23 项通过，3 项系统副作用测试忽略；
- TypeScript 通过；
- Vitest 16 个文件、64 项通过；
- ESLint、Prettier、生产构建通过；
- Playwright 9/9；
- Rust 格式与 Git 差异检查通过。

## 真实 CUDA 验收

配置：

- Worker：正式相邻 Worker；
- 模型：`large-v3-turbo`；
- 设备：CUDA GPU 0；
- 精度：FP16；
- Preset：`cn2`；
- 输入：`chinese_short.wav` 与 `programming-thinking-intro-45s.mp4`；
- 输出：独立自定义目录下 TXT。

结果：

- `task.queued.input_count = 2`；
- 两个媒体均完成，任务终态为 `task.completed`；
- 两个输出真实存在；
- 第一个媒体写入输出后第二个媒体开始转录；
- 前端累计序列单调不减；
- 总耗时 3.279 秒。

输出 SHA-256：

- `chinese_short.txt`：`AB9BFF2E61B06DF8E49C20D483BE49A284C39D1E18089D1192F885C7634498FF`
- `programming-thinking-intro-45s.txt`：`E285DB130F92B6F0AE227754D6C43B568D0C1F431F40D719ABCF02F1CEFFA6B3`

完整记录：

- `build\batch23-validation\runs\20260724-192655\validation.json`
- `build\batch23-validation\runs\20260724-192655\worker-events.json`

## 正式桌面

- Desktop SHA-256：`1608C27AB0F1F79F13BE22367B1A6A153A8E0325FBB4DC77879C4495014D8FF0`
- Worker SHA-256：`D3BBC800CA66AD2B88E19F47F7E515274D53728A040E8D80FC6E099FC9F87866`
- Desktop 正确拉起相邻 Worker，正常关闭且退出码 0；
- 无残留进程；
- 未生成安装程序或发布包。

## 结论

剪贴板入口只读取当前 Windows 文本并交由正式路径检查；多媒体整体进度按真实媒体位置累计且不会回跳；任务来源不再挤入完整路径；性能任务卡与任务监控显示一致的真实元数据。Worker、Desktop IPC v1、转录算法、Preset 和输出语义保持不变。
