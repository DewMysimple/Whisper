# 功能测试 014：复杂场景识别诊断与 V3 温度校准

日期：2026-07-26  
状态：自动化、完整 429 秒双模型 CUDA 矩阵、Hotwords 对照及冻结 Worker 验收通过；默认温度保留

## 样本与判定边界

- 完整媒体：用户第二轮 Large V3/Turbo 中文防幻觉对照所使用的约 429 秒复杂媒体。
- 可信中文基线：`C:\Users\Administrator\Desktop\实战测试\场景测试2：标准文本.md`。
- 英文锚点采用用户提供的人工校对稿，但用户已明确说明争议对话部分仍可能存在少量标注偏差，因此英文争议段不单独决定默认参数。
- 完整事实报告：`C:\Users\Administrator\Desktop\Obsidian仓库\DeepLearning\DataBase\凯尔特柚\Vedio\Markdown测试2\场景测试2（第二轮）：LargeV3-vs-LargeV3Turbo-事实比对报告.md`。
- A/B 产物与 JSON 报告位于 `build\batch28-validation`；所有输出写入独立 build 目录，未覆盖用户文件。

验收门槛：

1. 候选应减少幻觉或重复；
2. 中文字符 CER 不得恶化超过 0.5 个百分点；
3. 英文保留和术语命中不得退步；
4. 多个候选接近时才选择较低温度；
5. CER 下降若来自漏段或长重复，判定为不通过。

## 真实诊断验证

质量诊断来自 faster-whisper 的真实 segment，而不是模拟分数：

- `start/end`
- 实际 `temperature`
- `avg_logprob`
- `compression_ratio`
- `no_speech_prob`

阈值使用任务冻结后的实际参数。任务详情每媒体最多保存 12 条、每任务最多 50 条；Worker 日志保留完整诊断。诊断只提示复核，不修改输出。

冻结 Worker 对 20 秒中文短媒体执行 CUDA FP16：

| 项目 | 结果 |
| --- | --- |
| 终态 | `task.completed` |
| 主语言 | `zh` |
| 主语言概率 | `0.96630859375` |
| segment 数 | 7 |
| 温度回退 | 0 |
| 需复核片段 | 0 |

## 429 秒温度矩阵

| 模型 | 方案 | 中文字符 CER | 英文锚点 | 术语命中 | 回退/复核 | 判定 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| Large V3 | 当前完整阶梯 | 10.6024% | 0 | 2 | 11/11 | 基线 |
| Large V3 | 最高 0.6 阶梯 | 9.2169% | 0 | 2 | 5/5 | 同一区域仍产生低概率错误内容 |
| Large V3 | 固定 0 | 7.5301% | 0 | 2 | 0/1 | 高压缩长重复并漏段，不通过 |
| Turbo | 当前最高 0.6 阶梯 | 11.5663% | 2 | 2 | 11/11 | 基线 |
| Turbo | 最高 0.4 阶梯 | 10.6024% | 2 | 2 | 1/1 | Tofu 幻觉仍在，且有漏段 |
| Turbo | 固定 0 | 10.6024% | 2 | 2 | 0/1 | 与最高 0.4 输出字节完全一致，同样不通过 |

Large V3 固定 0 在约 55.78–67.29 秒生成同一句连续重复，压缩率达到 `7.659574468085107`。这说明较低 CER 不能解释为整体识别质量提升。

Turbo 最高 0.4 与固定 0 的 SHA256 完全相同：`0CABAE083AEF711F7783A33E7DA08E7DFE754B82B0D4943AD418387BA2659868`。两者都保留“谢谢你和的 Tofu”重复，未满足减少已知幻觉的首要条件。

## Hotwords 对照

取证词：

`拟态、摆拍、传媒学、奖学金、肄业、Walter Lippmann、simulacra-self、Synchronic Comparison`

| 模型 | 无 Hotwords | 启用 Hotwords | 结论 |
| --- | ---: | ---: | --- |
| Large V3 | 命中 2 | 命中 4 | 新增拟态、奖学金命中，但触发 13 段回退/复核及明显长重复 |
| Turbo | 命中 3 | 命中 2 | 术语命中下降，并出现新的错拼和普通中文退化 |

Hotwords 在单一样本上有时能提高个别词命中，但不能保证整体准确率，也可能改变解码路径并放大重复。正式软件保留按任务配置，不写入样本词默认值。

## 默认值结论

本轮没有任何候选稳定通过全部门槛，因此：

- `large-v3 + cn2` 保留当前温度阶梯；
- `large-v3-turbo + cn2` 保留当前最高 0.6 阶梯；
- 其他 Preset 完全不变。

这不是“未执行调参”，而是根据完整媒体证据拒绝把局部数字改善误当作安全默认值。后续如要改变默认值，应增加多个复杂媒体和独立人工标注。

## 自动化结果

| 范围 | 结果 |
| --- | --- |
| Python | 332 passed |
| Rust | 32 passed，2 ignored |
| Vitest | 85 passed |
| Playwright | 10 passed |
| TypeScript / ESLint / Prettier | 通过 |
| Vite production / Tauri release | 通过 |
| 冻结 Worker CUDA FP16 | 通过 |
| 正式 Desktop 拉起 Worker | 通过，无残留进程 |

本轮没有生成安装程序，也没有执行 Git 暂存、提交或推送。
