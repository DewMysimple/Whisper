# 第 25 次修改：媒体时长、SRT 伴随 TXT 与运行中交互保护

执行日期：2026-07-25。  
授权方式：用户批准“小改清单 9”连续执行计划，由 Agent 自行设置 CP0–CP5 检查点并持续完成。

## 完成内容

- 新增 Desktop IPC v1 向后兼容命令 `media.inspect`。正式 Worker 使用随包 PyAV 读取容器时长，按规范化路径、文件大小和修改时间做会话缓存；提交任务时重新检查并把逐媒体时长冻结进任务。
- 媒体队列、文件夹合计、任务历史、详情、任务监控与性能监控统一展示已知时长；无法可靠读取时显示“未知”，不伪造数据。
- SRT 工作区固定成组生成 `.srt` 与 `.txt`，两个文件位于同一输出目录且内容逐字节一致；冲突预检、覆盖、跳过、自动重命名、预留、续接、输出计数和目录定位均把二者视为一组。
- 有排队或运行任务时，模型卡与全部硬件编辑/应用/恢复入口锁定；状态层也拒绝修改。刷新模型与打开模型目录保持可用。
- 性能监控补充当前媒体序号，任务监控补充当前媒体进度、文件名、耗时和媒体总时长；侧栏徽标改为当前运行任务尚未完成的媒体数。
- 媒体列表与 Worker 日志采用可中断的自动追踪：人工滚动暂停，最后一次操作 8 秒后恢复；减少动态效果时即时定位。
- Rust Host 与前端移除 200 行日志裁剪，保留当前 Desktop 会话全部有效日志；复制、导出和统计使用完整缓冲区。
- 新增日志“清空”4 秒原位二段确认，同时清空 Host 与前端；广播顺序与写入共用锁，避免并发新日志被迟到清空事件误删。
- 点击窗口关闭按钮时，Rust Host 根据真实 Worker 事件维护的活动任务集合判断：无任务直接关闭；有任务使用 Windows 原生“是/否”确认且默认“否”。确认退出继续复用安全停止 Worker、清空队列及取消关机倒计时链路。

## 视觉与交互

继续使用现有主题、默认橙色和自定义强调色 Token。自动追踪遵循即时反馈、空间连续性、用户操作可中断及减少动态效果规则；未修改第三阶段 `WhisperUI`。

## 契约边界

- Desktop IPC 版本仍为 1；`media.inspect`、`task.queued.media_durations_seconds` 均为向后兼容扩展。
- 未修改四个 Preset 的参数语义、模型推理、CUDA 策略、SRT 词级时间轴或普通 TXT/Markdown 内容。
- SRT 伴随 TXT 是本次明确授权的新输出规则。
- 未暂存、提交、推送、创建分支或生成安装程序。

## 验收

- Python：299 项通过。
- Rust：28 项通过，2 项需人工声音/真实 GPU 的忽略测试；真实 GPU 另以正式冻结 Worker 完成。
- 前端：TypeScript、ESLint、Prettier、生产构建通过；Vitest 74 项通过；Playwright 10 项通过。
- 真实 PyAV：`chinese_short.wav` 为 `5.32263 s`，45 秒 MP4 为 `45.1 s`。
- 真实 CUDA/FP16：`cn`、`cn2`、`en_v1`、`en_v2` 四个 SRT 预设均完成；每组 `.srt/.txt` SHA-256 相同。
- 正式 Desktop 启动后同时拉起正式 Worker，关闭主窗口后两者正常退出且无残留进程。
- Windows 活动任务关闭确认的状态判断、默认否按钮和安全退出链路已完成代码与编译验收；未在自动化中实际点击“是”终止用户任务。

正式文件：

- Desktop SHA-256：`4131FAD932D10B726E1F316A2778ABE06A41964530D9A82C817C15F8C65A176A`
- Worker SHA-256：`99FD1922A7FE5263FDC1C30850690DF5538FC5F54CA51635D980837BCCB48267`
- 真实验收记录：`build\batch25-validation\runs\20260725-030630\validation.json`

## 检查点与保护

CP0 恢复点位于 `build\internal-checkpoints\cp0-batch25-20260725`，含正式二进制基线副本。执行期间未触碰 `.workbuddy`；`.claude/settings.local.json` 的既有删除保持原状。其余用户既有本地修改未被清理、暂存或提交。
