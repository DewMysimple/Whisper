# 第四阶段批次 0：UI 快照冻结与回迁边界执行报告

执行日期：2026-07-17。

用户授权：`执行第四阶段批次0`。

状态：`Implementation Complete / User Acceptance Pending`。

## 批次目标

在不修改正式产品代码和第三阶段工作区的前提下，建立第四阶段记录入口，冻结本轮 UI 快照，登记正式工作区保护基线，完成页面映射、演示数据排除和功能契约保护定义。

## 使用的 UI 快照

- 名称：`UI-2026-07-17-R01`。
- 来源：`C:\Users\Administrator\Desktop\WhisperUI` 当前模块化源码与 2026-07-17 17:20:02 生成预览。
- 清单摘要：`EBE0C3D566BFBC50228F3057E0D7673B2F421A280CF02452FEA41504B1B473E2`。
- 根预览与 `dist` 预览 SHA-256：`6F897F3034285266EC9A142D94B037B82A5A3D920E909016085E1B42A8E09B1F`。
- 高清主页背景 SHA-256：`D7BA11FAB859A304F548F3E0B60DD933EB8A44DC51CC536BDF3692CE5121146B`。
- Logo SHA-256：`1AD04589B6072733667BFB0A1D8C6B0B4DCF45236B92373E9CFFF4666B319D15`。

详细文件清单见[UI 快照记录](../../UI快照-UI-2026-07-17-R01.md)。批次执行期间没有运行 `build-preview.ps1`，因此没有改写第三阶段预览。

## 本批建立的记录

- `Log\40-第四阶段-UI成果回迁与正式应用\README.md`
- `Log\40-第四阶段-UI成果回迁与正式应用\UI快照-UI-2026-07-17-R01.md`
- `Log\40-第四阶段-UI成果回迁与正式应用\正式工作区保护基线.md`
- `Log\40-第四阶段-UI成果回迁与正式应用\页面与正式组件映射.md`
- `Log\40-第四阶段-UI成果回迁与正式应用\第一次UI修改\批次0-UI快照冻结与回迁边界\执行报告.md`

没有修改既有 `Log\README.md`，也没有创建产品代码、测试或构建产物。

## 涉及的页面和正式组件

本批只完成映射，不修改组件：

- 个人主页：当前正式工程无对应组件，计划在批次 1 新增纯表现组件。
- 转录工作台：`App.tsx`、`SourcePanel.tsx`、`PresetPanel.tsx`、`OutputPanel.tsx`。
- 任务记录：`TasksView`、`TaskList.tsx`、`TaskDetail.tsx`。
- 性能监控：`PerformanceStrip.tsx` 中的 `PerformanceView`。
- 偏好设置：`SettingsView.tsx`。
- 应用外壳：`App.tsx`、`Sidebar.tsx`、`styles.css`。
- 正式状态：`state/workspace.ts`、`state/persistence.ts`、`bridge/*` 和 `contracts/desktop.ts`。

完整映射见[页面与正式组件映射](../../页面与正式组件映射.md)。

## 未回迁的纯演示行为

批次 0 没有执行任何 UI 代码回迁。已明确禁止后续直接迁移：

- 模拟媒体、任务、统计、路径、输出和 Toast。
- 固定 Worker、IPC、模型、环境和日志状态。
- 模拟性能采样及正式契约没有提供的硬件详情。
- 原型 Preset 参数、默认 Beam 6、默认自定义状态。
- 原型 TXT/Markdown 默认值和固定输出目录。
- 15 秒/80 列性能时间语义。
- hash 导航、原型 localStorage 和静态 JavaScript 行为。
- 生成 HTML、构建脚本和 SVG sprite 的机械复制。

## 功能契约结论

本批没有修改任何产品实现，因此以下契约保持不变：

- Python Worker 和转录算法：不变。
- faster-whisper/CTranslate2 和 CUDA：不变。
- Desktop IPC v1：不变。
- Preset ID、参数与语义：不变。
- 后处理、文件命名、输出路径和输出内容：不变。
- 任务状态、历史、取消、重试和参数快照：不变。
- Tauri WebView2、本地 EXE、CSP、白名单能力和零端口架构：不变。

## Git 和工作区保护结果

批次开始时正式工程位于 `master`，HEAD 为 `bf1313afe7860660d367d71f9459b81dc4f461f9`，已有 13 个受跟踪修改和 2 个未跟踪项，没有 staged 变化。

本批没有触碰：

- `.claude/settings.local.json`。
- `.workbuddy`。
- 既有 `Log\README.md` 和第三阶段索引变化。
- 既有 `apps/web` UI、状态和 E2E 修改。
- `C:\Users\Administrator\Desktop\WhisperUI` 的任何文件。

已有前端文件的批次前 SHA-256 已登记在[正式工作区保护基线](../../正式工作区保护基线.md)，供后续批次区分既有变化和第四阶段增量。

## 验收与验证

已完成：

- 按规定读取正式工程与第三阶段上下文。
- 记录正式 Git 分支、HEAD、状态和既有文件哈希。
- 记录 UI 快照、预览、资产和模块化源码哈希。
- 建立页面—组件映射、可迁移项、适配项和禁止项。
- 建立批次 0–6 索引和每批强制日志规则。
- 确认本批不涉及产品功能契约变化。

未执行且不声称完成：

- Python、Vitest、Playwright 或 Rust 测试。
- Vite、Tauri、PyInstaller、NSIS 或便携包构建。
- WebView2、真实 Worker、CUDA、golden 或 EXE 验收。

这些验证不属于文档型批次 0，将在对应实现批次和批次 5 中按风险执行并记录。

## 批次 0 验收标准结果

- 第四阶段日志入口已建立：通过。
- `UI-2026-07-17-R01` 可由清单和哈希唯一识别：通过。
- 正式既有工作区已登记且未被覆盖：通过。
- 第三阶段工作区未修改：通过。
- 产品代码、测试、协议和构建产物未修改：通过。
- 功能契约保持不变：通过。
- 未暂存、提交、建分支、打标签、推送或发布：通过。

## 下一步门禁

批次 0 完成后停止。只有用户明确回复“执行第四阶段批次1”后，才能开始全局 Token、应用外壳、侧栏、顶栏、折叠、主题和个人主页的正式回迁。
