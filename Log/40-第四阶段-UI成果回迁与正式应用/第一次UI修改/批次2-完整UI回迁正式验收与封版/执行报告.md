# 第四阶段批次 2：完整 UI 回迁、正式验收与本轮封版

执行日期：2026-07-17。

用户授权：`那你执行下一次批次吧 最终的效果就是第三阶段的UI完整的在软件实现`。

采用快照：`UI-2026-07-17-R01`。

状态：`Implementation Complete / User Acceptance Pending`。

## 1. 批次调整

用户确认不再把剩余 UI 回迁拆成多个外部授权批次。因此原计划批次 2–6 合并为本批次 2，一次完成：

- 转录工作台、输入、Preset、参数、输出、真实媒体队列和启动卡。
- 任务记录、任务详情、性能监控和偏好设置。
- 正式状态、帮助、键盘、无障碍和响应式对齐。
- 前端、Python、Rust、E2E、Tauri 构建与桌面启动烟测。
- 差异审计、日志整理和本轮 UI 回迁候选封版。

合并只减少授权和日志开销，没有取消内部契约检查与测试门禁。

## 2. 实现范围

### 转录工作台

- 新增 `apps/web/src/components/LaunchCard.tsx`，将冻结快照的深色媒体队列与启动卡映射到正式 `inputs`、输出策略、Worker 状态和 `startTask`。
- `SourcePanel.tsx` 对齐输入区、路径输入和帮助层级；媒体条目改在启动卡真实队列中展示，移除操作仍调用正式状态。
- `PresetPanel.tsx` 保留四个正式 Preset 的真实 ID、参数和值，只回迁卡片、选中态和参数快照帮助。
- `OutputPanel.tsx` 保留正式输出目录选择、TXT/Markdown、冲突策略和兼容模式，只对齐快照视觉与帮助。
- `App.tsx` 使用正式 `LaunchCard`，没有引入原型模拟队列或模拟启动行为。

### 任务记录

- 任务页新增全部、运行、完成和需要处理四项汇总，全部由正式 `tasks` 实时计算。
- `TaskList.tsx` 对齐搜索、筛选、进度、状态、操作和展开页面结构，并修正正式操作按钮的网格布局。
- `TaskDetail.tsx` 的参数快照、输出预览、重试和资源管理器定位行为保持原实现；只由全局样式完成材质对齐。

### 性能监控

- `PerformanceStrip.tsx` 对齐趋势图、指标选择、硬件概览、运行链路和指标详情结构。
- 新增的指标详情只展示正式契约已有字段：当前读数、计算设备、采样来源、采样数量、时间、推理速度和 Worker PID。
- 原型中的温度、频率、功耗、风扇、驱动和 CUDA 版本没有正式 IPC 字段，因此没有伪造进入产品。

### 偏好设置

- `SettingsView.tsx` 对齐桌面外观、本地运行环境、便携配置和 Worker 日志布局。
- 启动模式、Worker、Desktop IPC、模型与 Python 环境均读取正式状态。
- 主题、配置导入导出、重启 Worker 和清除历史继续调用既有正式动作。

### 全局体验

- `styles.css` 完成第三阶段页面级视觉体系：卡片、输入、Preset、深色启动区、任务汇总、性能详情、设置、任务抽屉、响应式、高对比度、减少透明度和减少动态。
- `HelpTip.tsx` 继续作为正式可访问帮助组件；帮助只解释行为，不产生模拟状态。
- `App.test.tsx` 与 `desktop-workspace.spec.ts` 增加真实媒体队列、任务汇总、性能详情、本地运行环境及各页面截图覆盖。

## 3. 第三阶段 UI 完整回迁结论

以下页面和外壳均已在正式 React/Tauri 软件中实现：

- Mysimple 个人主页。
- 全局 Token、明暗主题、应用外壳、侧栏、顶栏和可逆折叠。
- 转录工作台、输入区、Preset、参数、输出策略、媒体队列和启动区。
- 任务概览、搜索筛选、任务列表与任务详情抽屉。
- 性能趋势、指标切换、资源概览、运行链路和指标详情。
- 桌面外观、本地运行环境、便携配置和 Worker 日志。
- 帮助、键盘焦点、减少动态、减少透明度、高对比度和主要响应式布局。

“完整回迁”指第三阶段的视觉语言、页面结构和适用于正式产品的交互已经完成映射；不包括把第三阶段的模拟数据和浏览器演示代码写进正式产品。

## 4. 明确未迁入的原型演示内容

- 模拟 Worker、模型、IPC、环境、任务、性能、日志、Toast、路径和输出数据。
- 固定媒体队列、固定任务统计和固定输出目录。
- 原型 Preset 参数、默认 Beam、TXT/Markdown 默认值或冲突行为。
- 原型性能页的温度、功耗、频率、风扇、驱动等非正式契约字段。
- 原型哈希路由、浏览器产品入口、演示 `localStorage`、生成脚本和整页 JavaScript。

这些差异是正式功能保护结果，不属于 UI 缺失。

## 5. 功能契约保护

- Python 转录算法、faster-whisper/CTranslate2 和 CUDA：未修改。
- Desktop IPC v1 schema、命令、事件、错误和生命周期：未修改。
- Preset ID、参数值和语义：未修改。
- 后处理、文件命名、输出路径、输出内容和冲突策略语义：未修改。
- 任务入队、取消、重试、历史、参数快照和输出预览契约：未修改。
- Tauri WebView2、本地 EXE、零端口、CSP 和能力白名单：未修改。
- 没有增加外部浏览器、localhost、网络服务、通用 shell 或外部联系方式跳转。

## 6. 测试和构建结果

全部实际执行结果：

- `corepack pnpm check`：通过。
  - Prettier：全部匹配。
  - ESLint：通过。
  - TypeScript：通过。
  - Vitest：4 个文件，10 项测试通过。
- Playwright E2E：4/4 场景通过，包含生产 Vite 构建。
- 人工截图复核：个人主页、工作台媒体队列、明暗主题、任务、性能详情、设置、侧栏展开与折叠均通过。
- 项目 Python 环境 `whisper_env`：236 项测试通过，用时 4.01 秒。
- Rust：9 项通过，0 失败；1 项真实 NVIDIA GPU smoke 按测试声明忽略。
- Tauri Release `--no-bundle`：构建成功。
- 生成 EXE：`apps/desktop/src-tauri/target/release/whisper-subtitle-desktop.exe`。
- 桌面启动烟测：进程保持运行、`Responding=True`、窗口标题 `WhisperSubtitle`；验证后仅终止本次启动的测试进程。

补充说明：默认系统 Python 缺少 `pytest`，首次命令未启动测试；随后使用工程自带 `whisper_env` 成功完成全部 236 项测试。

未执行且不声称完成：

- 需要真实 NVIDIA GPU 和项目运行条件的 Rust GPU smoke。
- NSIS 安装包、完整便携发布包或发布流程。
- 真实媒体的长时间 CUDA 转录人工验收。

本批次只执行无安装包桌面 Release 构建，没有发布产品。

## 7. 工作区与 Git 保护

- 分支：`master`。
- HEAD：`bf1313afe7860660d367d71f9459b81dc4f461f9`。
- 没有 staged 变化。
- 没有暂存、提交、创建分支、标签、推送或发布。
- `.claude/settings.local.json`、`.workbuddy`、既有 `Log/README.md` 和第三阶段索引变化的长度与时间戳保持批次 0 保护基线不变。
- 批次前已有的正式组件修改得到保留，没有恢复或覆盖用户工作。
- `C:\Users\Administrator\Desktop\WhisperUI` 未修改；其既有 dirty 状态保持不变。
- 来源与正式工程中的两个 Mysimple 资产 SHA-256 完全一致。
- 没有恢复 `.planning`，没有删除文件。

本批次实际新增或继续修改的正式前端文件：

- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/e2e/desktop-workspace.spec.ts`
- `apps/web/src/components/LaunchCard.tsx`
- `apps/web/src/components/SourcePanel.tsx`
- `apps/web/src/components/PresetPanel.tsx`
- `apps/web/src/components/OutputPanel.tsx`
- `apps/web/src/components/TaskList.tsx`
- `apps/web/src/components/PerformanceStrip.tsx`
- `apps/web/src/components/SettingsView.tsx`
- `apps/web/src/styles.css`

Python、Rust、Desktop IPC 合约和 Preset 数据文件没有修改。

## 8. 封版状态与后续门禁

`UI-2026-07-17-R01` 已形成第四阶段正式应用候选封版。当前等待用户验收。

- 不自动采用第三阶段后来产生的任何变化。
- 若用户验收当前成果，不需要再执行原计划批次 3–6。
- 若用户要求采用第三阶段新成果，必须冻结新的 UI 代码快照或明确差异，并建立新的“第 n 次修改”记录。
- 若用户只指出当前快照实现偏差，可在本轮验收修正范围内处理，不得顺手扩展功能契约。

## 9. 验收修正 1：性能热力图 80 × 20

用户在正式 EXE 验收时指出：第三阶段快照中的性能热力图为 20 个正方形高，而正式应用仍显示旧实现。

根因确认：

- 第三阶段 `performance.html` 明确标注为 `80 乘 20 个正方形采样格`。
- 第三阶段 `performance-demo.js` 使用 `rows = 20`。
- 第三阶段 `performance.css` 使用 80 列、20 行及 `aspect-ratio: 1 / 1`。
- 正式 `PerformanceStrip.tsx` 错误保留了旧的 60 列、14 行硬编码，`styles.css` 也仍使用 14 行固定高度。
- 这是第四阶段回迁遗漏，不是正式功能契约限制。

已修正：

- 正式热力图改为 80 列、每列 20 个格子。
- 每个格子使用 `aspect-ratio: 1 / 1`，确保为正方形。
- 正式性能历史仍保留 60 次真实采样；80 列网格右对齐展示这些采样，其余列为空，不改变 Desktop IPC v1 或采样语义。
- E2E 新增结构断言：必须存在 80 个 `.heat-column`，每列必须包含 20 个格子。

修正后验证：

- `corepack pnpm check`：通过，Vitest 10/10。
- Playwright E2E：4/4 通过，80 × 20 结构断言通过。
- 人工截图复核：20 个正方形高，与第三阶段快照一致。
- Tauri Release `--no-bundle`：重新构建成功。
- 更新后的 EXE：`apps/desktop/src-tauri/target/release/whisper-subtitle-desktop.exe`，构建时间 2026-07-17 23:43:27。
- 更新后的 EXE 启动烟测：`Responding=True`，窗口标题 `WhisperSubtitle`；验证后仅终止本次测试进程。

本次验收修正没有修改 Python、Rust、Desktop IPC、Preset、任务或输出代码；第三阶段工作区和既有本地修改仍未触碰。

## 10. 验收修正 2：移除工作台“最近任务”区

用户继续验收时指出：第三阶段冻结快照的 `workspace.html` 没有“最近任务”区，但正式工作台底部仍额外渲染了该组件。

根因确认：

- 第三阶段工作台只包含输入、Preset、输出和启动媒体队列。
- 正式 `WorkspaceView` 为方便访问任务而额外保留了 `<TaskList />`。
- 任务功能虽然仍然真实，但该额外区域不属于指定 UI 快照，是第四阶段视觉映射越界。

已整改：

- 从正式工作台移除 `<TaskList />` 和对应 `recent` 网格区域。
- “任务记录”页面继续保留任务汇总、搜索、状态筛选、取消、重试、输出定位和任务详情，不删除任何正式功能。
- E2E 与单元测试新增断言：转录工作台不得出现“最近任务”标题。
- E2E 在任务启动后切换到“任务记录”页面验证新任务，再返回工作台验证该区仍不存在。

整改后验证：

- `corepack pnpm check`：通过，Vitest 10/10。
- Playwright E2E：4/4 通过。
- 人工工作台截图复核：页面只保留输入、Preset、输出、媒体队列和启动区，与第三阶段 `workspace.html` 一致。
- Tauri Release `--no-bundle`：重新构建成功。
- 更新后的 EXE：`apps/desktop/src-tauri/target/release/whisper-subtitle-desktop.exe`，构建时间 2026-07-17 23:48:16。
- EXE 启动烟测：`Responding=True`，窗口标题 `WhisperSubtitle`；验证后仅终止本次测试进程。

本次整改没有修改 Python、Rust、Desktop IPC、Preset、任务数据或输出契约；只是把任务 UI 恢复到第三阶段指定的独立页面位置。

## 11. 验收修正 3：迁入功能区统一 +2px 字体模块

用户指出第三阶段曾明确要求功能区字体统一增加 2px，但正式软件内字号仍与 HTML 不一致，并要求后续回迁以 UI 源码而非截图为依据。

代码审计确认：

- 第三阶段 `src/styles/content-typography.css` 定义 `--content-font-bump: 2px`。
- 该文件逐项覆盖工作台、任务、性能和设置页面的标题、按钮、提示、参数、队列、任务、图表和日志字号。
- `build-preview.ps1` 将该模块放在 `apple-design.css` 之后，作为最后的 CSS 级联覆盖。
- 正式 `styles.css` 此前没有迁入该模块，因此功能区仍使用基础字号。
- 这是整个字体模块的迁移遗漏，不是 WebView2、系统缩放或字体渲染差异。

已整改：

- 在正式 `.view-content` 中增加 `--content-font-bump: 2px`。
- 按第三阶段 `content-typography.css` 的选择器和值迁入全部适用字号覆盖。
- 将原型 `.prototype-stat` 等价映射为正式 React `.task-summary-card`。
- 为正式 React 额外存在的任务自定义状态标签应用同样的 +2px 规则。
- 侧栏、顶栏和个人主页不属于该“四页功能区字体”模块，没有被错误放大。

代码级验收新增浏览器最终计算值断言：

- 工作台次级按钮：14px。
- 参数字段标签：12px。
- 输出脚注：11px。
- 任务汇总数字：26px。
- 性能指标数字：21px。
- 设置标签：13px。

验证结果：

- `corepack pnpm check`：通过，Vitest 10/10。
- Playwright E2E：4/4 通过，全部字体计算值断言通过。
- Tauri Release `--no-bundle`：重新构建成功。
- 更新后的 EXE：`apps/desktop/src-tauri/target/release/whisper-subtitle-desktop.exe`，构建时间 2026-07-18 00:15:51。
- EXE 启动烟测：`Responding=True`，窗口标题 `WhisperSubtitle`；验证后仅终止本次测试进程。

同时建立[第四阶段 UI 代码迁移与验收规则](../../代码迁移与验收规则.md)：以后以 HTML 结构、CSS 模块、`build-preview.ps1` 级联顺序和代码断言为主，截图只用于辅助检查溢出与整体布局。

本次整改没有修改 Python、Rust、Desktop IPC、Preset、任务或输出契约，也没有修改第三阶段工作区。
