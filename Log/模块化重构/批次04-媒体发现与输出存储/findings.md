# 批次 4 发现

- 总计划要求新建 `infrastructure/media_files.py` 和 `infrastructure/output_store.py`，定义 `OutputPlan`，并通过同目录临时文件与原子替换避免半文件。
- 批次只处理文件系统边界；模型、后处理、输出内容和现有入口必须保持兼容。
- 当前为普通 `master` checkout；批次 0–3 未提交成果是本批前置，因此继续原地执行。
- 批次开始业务源码为 22 个 Python 文件，内容聚合 SHA256 为 `62BC59CC211453E16992C40562858800661DA8EEF87E0F62338D014FF48D7514`；完整测试为 `178 passed in 1.40s`。
- 四个 Core 的媒体扩展名集合、递归 `rglob('*')`、大小写无关后缀判断和路径排序完全重复；异常文案相同，可抽取发现算法并由旧 Core 保留 CLI 展示。
- 四个 Core 的主输出、强制输出备份、桌面 TXT/MD 路径和 `open()` 写入结构完全重复，仅标准版与防幻觉版的完成日志是否显示句数不同。
- 现有 `txt_to_md` 不使用 `video_path`，实际契约是按 UTF-8 读取并原样写为另一扩展名；旧名称仍需接受第三个未使用参数。
- 旧 `txt_to_md` 还隐式返回 `None`；基础设施复制函数可返回目标路径，但四个旧包装器必须丢弃该返回值。
- 强制输出目录在模型加载前创建，自动 Text 目录在单文件转录前创建，桌面目录在转录成功后创建；接入新模块时要保持这些失败与副作用时序。
- 新 `OutputPlan` 只计算路径、不触碰磁盘；`prepare_output_plan()` 只提前建立主/备份目录，桌面目录由桌面写入阶段按旧时序创建。
- 每个最终文件都通过同目录命名临时文件写入、刷新、`os.replace()`；替换失败时既有最终文件不变，临时文件在 `finally` 中删除。
- 24 次真实 benchmark 全部通过，参数、展示标签和四份原 golden 输出不变；八组总耗时中位数比值为 1.004–1.038，低于 1.20 门禁。
- 固定中文音频 cn/cn2 均匹配独立 golden，真实输出目录中没有遗留 `.tmp`；最终完整测试为 `194 passed in 1.41s`。
- 环境自检、Console/模块/四旧 Core 帮助入口，以及 Console/模块两种 GUI 离屏启动全部通过。
- 最终业务源码为 25 个 Python 文件，内容聚合 SHA256 为 `301204574E422DF086DA1A98016F8C7559513FCA4129354A4006C5FADFE4E0BB`。
- 静态审计确认四个 Core 中 `open()`、`rglob()`、媒体扩展名集合、`mkdir()` 和 `Path.home()` 文件系统实现计数均为 0；四个旧 `txt_to_md` 都是单调用薄包装器。
- 收尾复核：暂存 0、`.workbuddy` 状态变化 0、临时文件/缓存/残留项目进程均为 0，执行 2 的 tracked 备份无差异。
