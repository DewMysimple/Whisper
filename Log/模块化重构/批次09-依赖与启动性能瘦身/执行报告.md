# 批次 09 执行报告

## 结论

批次 09 已完成。最终依赖方案移除 torch、torchaudio、torchvision，改用 `nvidia-cublas-cu12` 提供 Windows CUDA 12 的最小原生运行时；CTranslate2 负责 CUDA 能力判断，NVML 只负责设备元数据和显存监控。

全新隔离环境通过真实 CUDA 端到端验收，体积由 4.566 GiB 降至 1.318 GiB，减少 71.1%。四 preset 的 24 次 benchmark 输出完全稳定，总耗时中位数降至旧基线的 55.5%–58.0%。

## 代码与声明变更

- 新增 `src/whisper_subtitle/infrastructure/cuda_runtime.py`：注册 NVIDIA wheel 的 DLL 目录并预加载 cuBLAS。
- 更新 `src/whisper_subtitle/infrastructure/hardware.py`：CTranslate2 判断 CUDA/计算类型，NVML 补充名称和驱动版本。
- 更新 `requirements.txt`、`pyproject.toml`：删除 PyTorch 三件套，声明 `nvidia-cublas-cu12>=12.0,<13`。
- 新增 CUDA 运行时和依赖契约测试，更新硬件检测测试。
- benchmark 支持通过 `--cli` 指定隔离环境入口，避免候选误用当前环境。

## 验收结果

- 全新候选：`pip check` 通过，`246 passed`。
- 当前环境：`246 passed`，环境自检通过。
- 四 preset：冷/暖各 3 次，共 24/24 次通过。
- 中文真实音频：cn/cn2 与黄金文本一致。
- 英文 benchmark：en/en2 哈希均保持 `D6AD4418CE220D3981878CB84FB903B6E483FF2497FCBD6A9D5C65C15463DE78`。
- GPU：RTX 5070 Ti，float16；峰值显存约 2.32 GiB。
- GUI：offscreen 启动后存活 3 秒。
- 构建：生成 `whisper_subtitle-0.1.0-py3-none-any.whl` 成功。

## 仓库状态

- 未修改 `.workbuddy`。
- 未在现有 `whisper_env` 中卸载依赖。
- 未自动暂存、提交、打标签或推送 Git。
- 临时候选环境及测试输出已清理，benchmark 原始结果保留在本目录的 `candidate-runtime.json`。
