
---

## 第一批：Core 脚本（四个文件一次过）

```
请修改 src/core/ 下的四个脚本（WhisperProject.py、WhisperProject2.py、WhisperProjectCN.py、WhisperProjectCN2.py），修复以下四个问题，注意保持四个脚本之间的行为一致：

1. 文件扩展名校验：当前只接受视频扩展名（.mp4/.mkv/.avi/.mov/.wmv/.flv/.webm），请增加对音频格式（.mp3/.wav/.m4a/.aac/.ogg）的支持
2. 校验前置：将文件格式校验和输入路径检查移到模型加载（model_init）之前，避免无效输入白费模型加载时间（约5秒+显存）
3. 失败退出码：单文件转录异常被捕获后，main() 应返回非零退出码（如 return 1），而非正常返回 None
4. GBK 编码兼容：CLI 入口处增加 PYTHONIOENCODING=utf-8 或编码容错处理，确保在 Windows 中文 GBK 控制台下不会因 emoji 输出触发 UnicodeEncodeError

每修完一个脚本请做冒烟测试确认 --help 正常，修完四个后告诉我核心改动点。
```

---

## 第二批：test_env 补漏

```
请修改 src/utils/test_env.py，在 CORE_SCRIPTS 列表中补充第四个核心脚本 WhisperProjectCN2.py。
目前只检查了前三个脚本，启动自检无法发现 CN2 脚本缺失。
```

---

## 第三批：GUI 进程生命周期

```
请修改 src/gui/WhisperPyQtGUI.py，修复以下两个问题：

1. closeEvent 处理：窗口关闭时，若仍有正在运行的 QProcess，应主动 terminate/stop 子进程，不能遗留孤儿进程
2. "停止"操作：当前在主线程调用 waitForFinished(5000)，窗口最多冻结5秒。改为先 terminate，然后通过 QTimer 异步检查进程状态，超时后再 kill

注意：只改进程生命周期逻辑，不要动 UI 布局或其他业务代码。
```

---
