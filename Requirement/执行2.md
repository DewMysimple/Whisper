
---

## 第一批：建立 Python 包结构

```
请执行第二阶段工程收敛第 1 项：

建立 Python 包结构：
- 创建 pyproject.toml（使用 setuptools），项目名 whisper_subtitle，包目录 src/whisper_subtitle/
- 将现有 src/core/、src/gui/、src/utils/ 迁移到包内子目录：
  src/whisper_subtitle/core/
  src/whisper_subtitle/gui/
  src/whisper_subtitle/utils/
- 每个子目录放置 __init__.py
- 移除所有 sys.path.insert 和 sys.path.append，改为相对导入（from ..core import xxx）
- 删除旧 src/core/、src/gui/、src/utils/ 下的源文件（已移入包内）
- 更新 launch.vbs 中的路径指向新位置
- 确认 python -m whisper_subtitle 能正常启动

注意：不要动业务逻辑，只搬迁文件和改导入方式。改完做冒烟测试确认 --help 和 GUI 能正常启动。
```

---

## 第二批：单 CLI 入口 + 结构化日志

```
请执行第二阶段工程收敛第 2 项和第 6 项（包结构已就绪）：

2. 单 CLI 入口：
   - 创建 src/whisper_subtitle/cli.py
   - pyproject.toml 注册 console_scripts：whisper-subtitle = "whisper_subtitle.cli:main"
   - 支持三个子命令：
     whisper-subtitle transcribe <input> [-o OUTPUT] [--preset {cn,cn2,en,en2}]
     whisper-subtitle gui（启动图形界面）
     whisper-subtitle check（运行环境自检）
   - 四个原有 main() 保留不动，由 cli.py 按 preset 分发调用

6. 结构化日志：
   - 在 whisper_subtitle/__init__.py 中配置标准 logging
   - 搜索所有 except: pass 和 except Exception: pass
   - 逐一替换为 logger.warning() 或 logger.error()，至少记录异常类型和消息
   - 不要完全静默任何异常

每做完一项冒烟测试，告诉我状态。
```

---

## 第三批：统一 QFileDialog + QSettings 持久化

```
请执行第二阶段第 4 项和第 5 项，只改 src/whisper_subtitle/gui/WhisperPyQtGUI.py：

4. 统一文件选择框：
   - 将所有 tkinter.filedialog 替换为 QFileDialog
   - 移除所有 import tkinter 相关代码
   - 确认文件选择行为和之前一致

5. 引入 QSettings 持久化配置：
   - 持久化上次选择的输入路径、输出路径
   - 持久化上次使用的 preset 模式
   - 持久化窗口位置和大小
   - 启动时自动恢复

注意：只改 GUI 文件，不碰 core。改完确认 GUI 能正常启动，文件选择和之前行为一致。
```

---

## 第四批：清理依赖

```
请执行第二阶段第 3 项：

清理依赖：
- 重写 requirements.txt 为声明式顶层运行依赖（不要 pip freeze 快照）
- 分三组：
  # 运行依赖
  faster-whisper>=1.0
  pyqt5>=5.15

  # GPU 加速（可选）
  torch>=2.0
  torchaudio>=2.0
  torchvision>=0.15

  # 开发工具
  pytest>=7.0
- 移除 funasr、ttkbootstrap（当前源码未导入）
- 移除所有间接依赖，让 pip 自动解析
- 在修改 requirements.txt 之前，先用真实音频跑一次转录，记录输出结果作为回归基准
- 修改后重建虚拟环境，再跑一次转录确认输出一致
- 确认真实转录功能没有回退
```
