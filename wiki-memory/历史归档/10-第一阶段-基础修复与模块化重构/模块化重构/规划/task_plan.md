# 模块化重构规划任务

## 目标

基于前三阶段成果，制定一份可由用户逐批触发、每批可验证和回滚的模块化重构执行计划。

## 阶段

- [complete] 1. 汇总前三阶段成果与模块化前审计结果
- [complete] 2. 编排批次顺序、边界、验收标准和用户触发语句
- [complete] 3. 校验计划完整性并交付

## 约束

- 本次只编写计划，不修改业务代码。
- 所有文档写入 `F:/WhisperSubtitle/Log/模块化重构规划/`。
- 后续每个批次必须先通过自身验收门禁，才能进入下一批。

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 最终校验脚本将 `git diff --check` 命令直接嵌入 PowerShell 哈希表表达式，触发解析错误 | 1 | 将 Git 命令拆为独立语句，保存布尔结果后再构造状态对象 |
