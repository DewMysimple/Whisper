# 新架构批次 7 执行报告

执行日期：2026-07-16
状态：`Implementation Complete / User Acceptance Pending`

## 结论

下一代 Tauri 2 + React/TypeScript WebView2 + Python 常驻 Worker 已成为正式桌面入口。安装版和便携版均直接使用 Tauri EXE，VBS 启动层不再保留。Python CLI 继续提供 `transcribe`、`check` 和 `worker`；原 `gui` 子命令已退役。

PyQt5 表现层与两个专属测试文件已按用户约束送入 Windows 回收站，没有永久删除。PyQt5 已从 `pyproject.toml`、`requirements.txt` 和发布构建 requirements 移除；从零构建的 wheel metadata、冻结 Worker 和 CycloneDX SBOM 均不含 PyQt5。

## 入口与退役范围

- 安装版使用 Windows 快捷方式或安装目录 `whisper-subtitle-desktop.exe`。
- 便携目录直接运行同目录 `whisper-subtitle-desktop.exe`，不包含 VBS。
- `python -m whisper_subtitle` 无参数时显示 CLI 帮助，不再隐式启动 GUI。
- `python -m whisper_subtitle gui` / `whisper-subtitle gui` 返回 argparse 未知命令错误。
- 送入回收站：`src/whisper_subtitle/presentation/gui/`、`tests/test_gui_main_window.py`、`tests/test_gui_protocol.py`、根目录和便携发布中的 `launch.vbs`。
- 保留：Python CLI、console JSONL、Desktop IPC v1、常驻 Worker、历史 PyQt5 截图与历史执行记录。

## 发布构建

发布脚本从零创建 Python 3.14.2 环境，只安装固定 Worker/构建依赖并构建当前 wheel。`pip check` 返回 `No broken requirements found.`，wheel requirements 和 `sbom-python.cdx.json` 均未出现 PyQt5。

首次 NSIS 封装出现一次 `io: unexpected end of file`。检查发现原脚本的 `npx pnpm` 会临时解析到 pnpm 11；发布脚本随后改为 Corepack 固定工程 pnpm 10.34.5，并直接调用 Web 子包 build。回收半成品 setup 后复用完整新鲜 stage 重试，NSIS 构建成功。该过程没有改变应用依赖或转录实现。

最终 setup：

- 大小：727,596,950 bytes。
- SHA-256：`91940EDE1BB0806CFAC94AA874CCB7C2C62F06F4C5D349FA193C0CC1B40ADB0C`。
- Authenticode：`NotSigned`，因为没有提供签名证书。
- `SHA256SUMS.txt`：6/6 匹配。

## 真实验收

验证主机：Windows 11 专业版 x64、build 26200、NVIDIA GeForce RTX 5070 Ti。Windows 10 不在支持和验证范围。

### 正式入口

- 便携 `whisper-subtitle-desktop.exe` 与安装版 EXE 均创建标题为 `WhisperSubtitle` 的窗口。
- 运行时：打包 Worker 1、新 Python 进程 0、TCP 监听 0。
- 正常关闭后 Worker 0。

### 真实 GPU

- 便携版和安装版冻结 Worker 均使用 `cuda/float16`。
- 连续执行 `cn`、`cn2`，模型加载事件 1 次。
- normalized SHA-256：`83C3A8CAB6BDEB88CE0FF0B6C79C3BB73ADD9190E16388D17ECC05A0F4EF7ABD`。
- raw SHA-256：`AB9BFF2E61B06DF8E49C20D483BE49A284C39D1E18089D1192F885C7634498FF`。
- 便携版耗时 2.6362 秒，安装版耗时 2.4102 秒。

### 安装与卸载

- 最终 0.1.0 安装返回 0，桌面 EXE、Worker、1,617,884,929-byte 模型和卸载器完整。
- 安装版窗口启动/关闭、零 Python、零监听端口和真实 GPU 通过。
- 最终卸载返回 0；注册表项和安装目录均消失。
- WebView 用户数据保留，卸载前后文件数均为 297。
- 0.1.0 → 临时 0.1.1 覆盖升级已在获验收的批次 6 真实通过；本批保持相同 Tauri identifier、版本与安装布局，没有重复制造虚假新版本。

### 回归

- Python：`235 passed`。相较批次 6 的 245 项，减少的 10 项正好是退役的 PyQt5 专属测试；其余契约全部通过。
- 前端：Prettier、ESLint、TypeScript strict、Vitest `8 passed`、Playwright `3 passed`。
- Rust：rustfmt、严格 Clippy、Cargo test `9 passed, 1 ignored`；真实冻结 Worker GPU 由独立脚本覆盖。
- 环境自检与现有环境 `pip check` 通过。

## 未改变内容

- 未修改 faster-whisper/CTranslate2 转录算法。
- 未修改四个 preset、参数、后处理、文件命名和输出内容。
- 未删除 Python CLI、历史证据或原始需求。
- 未启用自动更新、浏览器 WebUI 或 localhost 服务。
- 未修改 `.workbuddy` 和本地 Agent 配置。
- 未执行 Git 暂存、提交、推送、标签或发布。

## 剩余发布限制

- 当前产物未签名；正式 Authenticode 需要用户提供代码签名证书。
- 自动更新仍为 P2，当前使用已验证的离线覆盖升级。
- 验证在同一台 Windows 11 GPU 实机的全新构建环境和冻结运行时完成，没有第二台物理干净机证据。

建议将批次 7 判定为“实现完成，等待用户最终验收”。本计划没有批次 8；后续新增功能应另行定义范围。
