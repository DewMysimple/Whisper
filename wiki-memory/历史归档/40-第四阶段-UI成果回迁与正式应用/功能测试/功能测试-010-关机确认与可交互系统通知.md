# 功能测试 010：关机确认与可交互系统通知

日期：2026-07-25  
状态：自动化、真实 CUDA、正式 Desktop 构建及启动烟测通过；下一次真实 Windows 通知需由用户点击复验窗口聚焦手感。

## 测试目标

1. 休眠入口和执行能力完全移除；
2. 错误横幅五秒自动清除；
3. 按钮与快捷键均不能绕过关机确认；
4. 同名冲突继续携带一次性关机授权；
5. 普通通知显示总耗时而不是任务名称；
6. 关机任务严格使用 60、30、15、5 秒四次通知；
7. 通知声音每次只请求一次，点击通知可返回软件。

## 自动化覆盖

### 前端

- 标准版确认后才出现关机确认；
- 防幻觉版本直接进入关机确认；
- 取消后媒体队列、关机选择和参数保持；
- 确认后只提交一次冻结草稿；
- 同名冲突二次提交沿用同一草稿和 `shutdown`；
- 休眠按钮不存在；
- 顶部错误提示在 5,000ms 后清除；
- 普通终态通知使用 `总耗时 MM:SS`；
- 关机 60 秒通知替代普通完成通知。

结果：

- Vitest：16 个文件、67 项通过；
- Playwright：10/10；
- TypeScript、ESLint、Prettier和生产构建通过。

### Rust Host

- 只允许 `shutdown`，拒绝已移除的 `hibernate`；
- 取消关机后假执行器不会执行；
- 成功队列进入倒计时，失败和新任务解除关机；
- 30、15、5 秒跨越检测各发送一次，重复轮询不重复发送；
- Windows 通知使用长时长、返回按钮和激活回调；
- 单个原生命令只播放一次提示音。

结果：26 项通过，2 项真实系统声音／GPU测试按设计忽略，Cargo fmt通过。

## 真实 CUDA

配置：

- 正式 Worker；
- `large-v3-turbo`；
- NVIDIA CUDA、GPU 0、FP16；
- `cn2`；
- 两个真实短媒体。

结果：

- 终态：`task.completed`；
- 输入数量：2；
- 两个 TXT 输出均存在；
- 进度序列单调不减；
- Worker SHA-256保持不变。

记录：

`F:\WhisperSubtitle\build\batch23-validation\runs\20260725-003403\validation.json`

## 电源安全

自动验收使用假关机执行器，未允许系统到达真实关机调用。正式 EXE 仅执行启动、拉起相邻 Worker、关闭主窗口和残留进程检查，Desktop/Worker均正常退出。

Windows 通知的原生点击动作无法由当前无 WinAppDriver、Tauri Driver 或 UI Automation 依赖的环境安全合成；代码路径、Rust 编译和窗口恢复调用已验收，最终手感由用户在下一条真实通知上点击确认。

## 正式产物

- Desktop SHA-256：`06E514AF1B373AEAD0595974EFD158A1911688E3DD7869BD1B959AC41CBABBB9`
- Worker SHA-256：`D3BBC800CA66AD2B88E19F47F7E515274D53728A040E8D80FC6E099FC9F87866`

