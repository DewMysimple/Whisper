# 第 31 次修改：UI 信息层级与视觉节奏整理

日期：2026-07-28  
状态：Completed / Visual QA / Formal Desktop Validated

## 授权与边界

用户授权在现有 React/Tauri 正式工程内系统整理字号、行距、组件间距、页面层级和长页面结构；不重写功能，不修改转录契约，并要求保留任务监控区域现状。第三阶段 UI 工程保持不动。

本轮开始前恢复并排除 `.claude/settings.local.json` 的本地删除，将第 21–30 次软件、测试与日志成果提交为：

- Git：`d4c5335cbd5ac77df235c09a2743ae95bc7db546`
- 提交信息：`feat: complete phase 4 modifications 21-30`

第 31 次修改只保留在未暂存工作区，等待用户另行确认后再提交。

## 实施结果

### 统一视觉基础

- 在正式 `styles.css` 末端建立本轮规范层，统一 1540px 内容上限、4/8/12/16/24/32 间距刻度、正文/辅助文字下限、行高、卡片圆角、边框和阴影。
- 普通工作台主区块统一为 24px 节奏，减少高分辨率下的无效留白、重复描边和重阴影。
- 主内容说明文字与紧凑控件的可读字号下限提高到 12px；字号仍通过 `--ui-font-size` 响应用户设置。
- 2560×1440、1920×1080、1440×900 和正式最小窗口 1180×720 均无横向溢出。

### 转录工作台

- 删除识别模式下方整块语言说明和模型参数摘要。
- 四个识别模式改用更紧凑的卡片高度；模型参数入口保留，并直接进入模型页“参数配置”标签。
- 启动与队列卡由深色孤立表面改为与工作台一致的浅/深主题表面，继续以橙色主按钮表达首要操作。
- 输入、输出、SRT、队列、快捷键和执行完成动作保持原行为。

### 模型与参数

- 新增“模型选择 / 参数配置”两个前端展示标签；标签状态不持久化，不写入配置或任务草稿。
- 模型选择只展示 Large V3 与 Large V3 Turbo 两个真实模型。
- 未开放的自定义模型降级为底部说明，不再伪装成可操作的同级模型卡片。
- 参数配置保留模型 × Preset 摘要及全部参数能力；切换标签不会丢失当前编辑值。

### 硬件、性能、历史和设置

- 硬件页重排为能力概览、推理设备、GPU/CPU 两列配置和底部操作栏；当前无关配置保留可理解说明并降低视觉权重。
- 性能页提高空闲提示、坐标、指标说明和次级状态的可读性；趋势图、诊断与任务进度未改变。
- 任务监控组件不改结构。历史记录改为单列结构化任务行，四张统计卡压缩为一条摘要带；搜索、日期/状态筛选、删除、续接、详情和输出操作均保留。
- “跟随系统”改为“跟随 Windows”，使用正常的自动主题预览和明确说明；主题、强调色、字体、字号及支持区重新对齐。

## 契约保护

- Desktop IPC 保持 v1。
- Python Worker、Preset 语义、识别算法、任务持久化、输出路径和文件规则均未修改。
- 本轮没有下载模型、改动正式 Worker、生成安装程序或发布包。
- 正式 Worker SHA256 保持 `0E9C97E3E5FB04EBB42C0C4E548E1C9717015D1060B9C60B5928CD6490A577B7`。

## 测试与视觉验收

- Prettier：通过。
- ESLint：通过。
- TypeScript：通过。
- Vitest：20 files / 87 tests passed。
- Playwright：11 passed；覆盖模型双标签、参数值保留、语言说明移除、跟随 Windows、历史单列、主题/字号、工作台与原有任务操作。
- Python：352 passed。
- Rust：33 passed，2 ignored；忽略项为有声通知和真实 NVIDIA GPU 人工测试。
- Vite production build：通过；保留既有的单个 JS chunk 大于 500 kB 非阻断提示。
- 视觉截图：`C:\Users\Administrator\.codex\visualizations\2026\07\28\019fa81e-f677-7622-87a1-d89aa0a28095`。
- 覆盖浅色、深色、跟随 Windows，以及 2560×1440、1920×1080、1440×900、1180×720；未发现横向溢出、标题/控件基线冲突或任务监控外观回退。

## 正式 Desktop

- 使用 `corepack pnpm desktop:build` 完成 Tauri Release `--no-bundle` 构建。
- 唯一正式入口：`F:\WhisperSubtitle\apps\desktop\src-tauri\target\release\whisper-subtitle-desktop.exe`
- Desktop SHA256：`3BB4D47265E9535939246595B16BA220585CE39F33DE6E5BA82E1162D63F41AF`
- 最终隐藏启动烟测 PID `9932`，成功拉起相邻 Worker PID `26192`；主窗口句柄有效，`CloseMainWindow` 正常关闭，Desktop/Worker 均无残留进程。
- `target\release\bundle` 与 `target\release\nsis` 均未生成。

## 验收结论

第 31 次 UI 整理已完成代码、自动化、响应式视觉、正式 Release 构建和桌面启动烟测。工作台的信息密度更均衡，模型与历史页面的主要任务更清晰，硬件设置和偏好设置的层级更稳定；任务监控及转录契约没有回退。

本轮源码与日志按授权保持未提交。下一次修改或 Git 提交必须由用户再次明确授权。
