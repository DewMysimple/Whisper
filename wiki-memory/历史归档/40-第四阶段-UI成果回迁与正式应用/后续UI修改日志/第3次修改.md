# 第 3 次修改：真实 15 秒性能窗口与正式 UI 反向抽离

执行日期：2026-07-18。  
状态：实现、第三阶段调和、自动测试、Tauri Release 构建与正式桌面烟测均已完成；等待用户目视验收。

## 1. 用户授权

用户明确要求：

> 先对性能页 `15s / 7.5s / 现在` 偏差执行修改，并且要重新抽离 UI 层到 `C:\Users\Administrator\Desktop\WhisperUI`，以目前的软件 UI 层为基准让 HTML 同步。

本次按一次授权、一次完整修改执行，内部检查点为：双工作区审计、正式 15 秒数据语义、第三阶段代码反向抽离、浏览器验收、正式桌面构建、进程烟测和日志封版。

## 2. 基线与调和方向

- 正式功能事实来源：`F:\WhisperSubtitle` 当前 React/Tauri/Worker/IPC 实现。
- 视觉调和来源：当前正式 React 组件和最终 `apps/web/src/styles.css`，不是截图。
- 调和输出：第三阶段 `UI-2026-07-18-R02`。
- 第三阶段的旧 CSS 模块未删除，但已从当前构建加载顺序退出，避免新旧两套级联同时覆盖。
- 正式 CSS 和抽离基线 SHA-256 均为 `DCA318DCB53A277B978570FE683ACB2777AB827394FB9068A2CC158B0FBA2D23`。

## 3. 正式性能页修复

原正式前端每 2 秒采样并最多保留 60 次，横轴使用“60 次前 / 30 / 现在”，与第三阶段 15 秒语义不一致。本次没有只换文案，而是：

- 新增带时间戳的性能窗口工具，按真实时间裁剪最近 15,000ms。
- 最大保留 80 个样本；前端目标轮询间隔为 `15000 / 79`，约 190ms。
- 若 Worker 响应较慢，不伪造样本；热力图按时间戳把实际样本映射到 80 列，可留下空列。
- 横轴精确显示 `15s / 7.5s / 现在`，说明为“时间范围 15s 至现在”。
- 当前会话显示真实保留样本数 `/80`，不再把采样次数冒充时间范围。

正式修改文件：

- `apps/web/src/state/performanceWindow.ts`（新增）
- `apps/web/src/state/performanceWindow.test.ts`（新增）
- `apps/web/src/state/workspace.ts`
- `apps/web/src/bridge/tauriDesktopBridge.ts`
- `apps/web/src/bridge/mockDesktopBridge.ts`
- `apps/web/src/components/PerformanceStrip.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/e2e/desktop-workspace.spec.ts`

## 4. 第三阶段反向抽离

本轮明确获准修改 `C:\Users\Administrator\Desktop\WhisperUI`：

- 将正式 `apps/web/src/styles.css` 逐字节抽离为 `src/styles/formal-ui-baseline.css`。
- 新增 `prototype-adapter.css`，只处理静态 HTML 缺少 React 状态时的最小适配。
- `build-preview.ps1` 只加载上述两份 CSS；旧 Token、页面和增量样式文件保留为历史，不删除、不加载。
- 依据正式 React 代码更新应用外壳、主页、工作台、任务、性能和设置 HTML。
- 补充错误提示、任务详情、正式图标结构和演示交互。
- 初始媒体队列为空；不再把静态媒体或“最近任务”伪装成正式状态。
- 性能详情使用正式字段结构，但值仍明确来自 `mock-data.js`，不声称实时检测。

本轮涉及的第三阶段文件包括 `build-preview.ps1`、`src/shell.html`、`src/components`、五个 `src/pages`、`src/scripts`、`src/mock/mock-data.js`、两份当前加载样式、生成预览及阶段文档。完整冻结标识见[R02 快照](../UI快照-UI-2026-07-18-R02.md)。

## 5. 纯演示排除与功能保护

- 未把 HTML 中的模拟性能、任务、Worker、模型、路径、媒体、日志、Toast 或配置行为写入正式 React 状态。
- 未修改 Python 转录算法、faster-whisper、CTranslate2、CUDA 或模型推理。
- 未修改 Desktop IPC v1 schema、消息、事件、错误或生命周期。
- 未修改 Preset 的真实参数和值、后处理、任务契约、文件命名、输出路径或输出内容。
- 未修改 Tauri 窗口尺寸、标题栏、安装策略或本地 EXE 产品形态。
- 未生成根 `dist`、便携版、安装程序、发布清单或模型副本。
- 未暂存、提交、创建分支、打标签、推送或发布。

## 6. 验收结果

正式前端：

- Prettier：通过。
- ESLint：通过。
- TypeScript：通过。
- Vitest：5 个测试文件、12 条测试全部通过。
- Web production build：通过。
- Playwright E2E：4 条全部通过。
- 新窗口单测证明 100 个、200ms 间隔样本会按 15 秒边界裁剪为 76 个真实样本，首尾时间戳为 4800/19800ms。

第三阶段：

- `build-preview.ps1`：通过。
- 根预览与 `dist/index.html` SHA-256 均为 `52BA1DA918721C99456E3FD01E1A5C468909F477C6E0D434CF1E52B21743F5C0`。
- 本地 Chrome DOM/计算样式检查：控制台 0 错误；五页可切换；性能 80 列 × 20 行、精确横轴和说明通过；任务详情通过；工作台初始空队列通过。

正式桌面：

- `corepack pnpm desktop:build`：成功，使用 Tauri `--no-bundle`。
- 唯一正式 EXE：`apps/desktop/src-tauri/target/release/whisper-subtitle-desktop.exe`。
- EXE SHA-256：`60069F9A1E7C8C96D0EE2A3AB089812405B84307F49D51CB3CD820F672C1B81E`。
- 正式 Worker 路径：`apps/desktop/src-tauri/target/release/worker/whisper-subtitle-worker.exe`；SHA-256 仍为 `E5A4D71F2CE34390F131B4F1D9ACAE44A551AFC7B52D880A4B46BD73F50E4E84`。
- 桌面 Host 与子 Worker 连续运行并保持 `Responding=True`；正式程序已保留运行供用户目视验收。
- 根 `F:\WhisperSubtitle\dist` 不存在；没有生成第二份桌面发布物。

本轮没有复跑 Python 全量、真实音频 golden 或 Cargo test：没有修改 Python、Rust、IPC、Preset、推理和输出源码，且第 2 次修改的冻结 Worker 二进制与哈希保持不变。Rust/Tauri 部分以 Release 重新编译和真实 Host/Worker 拉起覆盖本轮桌面集成风险。

烟测过程中曾误用不存在的 `target/release/whisper-subtitle-worker.exe` 路径检查并短暂报告 Worker 缺失；读取 Rust Host 后确认既定正确路径是 `target/release/worker/whisper-subtitle-worker.exe`。正确路径下文件、哈希和实际子进程均验证通过，没有执行恢复或替换。

## 7. 工作区保护

- 正式工程执行前已有第一次 UI 修改、文档、`.claude/settings.local.json` 和 `.workbuddy` 等大量本地变化；本轮没有回退、删除、移动、暂存或提交它们。
- 第三阶段执行前也已有大量用户未提交设计变化；本轮只在用户明确授权的 UI 调和范围内继续修改，没有清理或提交既有内容。
- 没有删除文件；因此没有永久删除或回收站操作。
- `packaging/worker.spec` 和 `tests/test_packaging.py` 是第 2 次修改的既有改动，本轮保留但未再次改变其语义。

## 8. 完成状态与下一次触发

正式性能页时间语义和真实数据窗口已对齐，第三阶段 HTML 已以当前正式 UI 代码重新建立共同基线。本轮等待用户在正在运行的正式 EXE 和第三阶段预览中进行目视验收。

以后用户继续在第三阶段修改后，仍需明确要求按最新源码回迁；不得自动同步。下一次实际 UI 或 UI 功能修改应建立 `第4次修改.md`。
