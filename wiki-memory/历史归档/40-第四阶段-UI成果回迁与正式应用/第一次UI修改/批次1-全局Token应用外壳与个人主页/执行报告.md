# 第四阶段批次 1：全局 Token、应用外壳与个人主页执行报告

执行日期：2026-07-17。

用户授权：`执行第四阶段批次1`。

采用快照：`UI-2026-07-17-R01`。

状态：`Implementation Complete / User Acceptance Pending`。

## 1. 批次范围

本批次只回迁第三阶段快照中的全局视觉 Token、正式应用外壳、侧栏、顶栏、折叠形态、主题入口和个人主页。工作台内容区、任务记录、性能监控、偏好设置和系统级验收仍分别留在后续批次。

本批次没有机械覆盖第三阶段 HTML、CSS 或 JavaScript，而是把视觉意图映射到正式 React 组件、正式路由状态和现有真实数据。

## 2. 实际修改文件与正式组件

### 正式产品代码

- `apps/web/src/App.tsx`
  - 接入个人主页视图、正式顶栏帮助提示和主题切换入口。
  - 保留现有宿主连接状态、任务搜索和正式工作台路由。
  - `Ctrl+0` 返回个人主页；现有 `Ctrl+1`–`Ctrl+4` 页面快捷键保持不变。
- `apps/web/src/components/Sidebar.tsx`
  - 映射宽侧栏、个人入口、导航激活态、引擎状态卡和可逆折叠形态。
  - Worker、模型、环境和任务数量继续读取正式状态。
- `apps/web/src/components/PersonalHome.tsx`
  - 新增 Mysimple 个人主页表现组件。
  - 联系方式只作为文本展示，不启动邮件客户端、浏览器或外部链接。
  - “进入工作台”只切换正式 React 视图。
- `apps/web/src/components/HelpTip.tsx`
  - 新增可复用的轻量帮助提示组件。
- `apps/web/src/state/workspace.ts`
  - 为正式视图联合类型增加 `home`，并以个人主页作为本快照默认入口。
  - 未改变任务、转录、Preset 或输出状态结构。
- `apps/web/src/state/persistence.ts`
  - 保留正式持久化主题语义，同时同步桌面页面的 `theme-color` 元信息。
- `apps/web/src/styles.css`
  - 映射全局明暗 Token、系统字体、层级、圆角、阴影、半透明材质、侧栏/顶栏、个人主页和折叠状态。
  - 增加减少动态、减少透明度和高对比度适配。
- `apps/web/index.html`
  - 将初始 `theme-color` 对齐到新浅色画布。
- `apps/web/src/assets/mysimple-home.jpg`
  - 从冻结快照复制；SHA-256 为 `D7BA11FAB859A304F548F3E0B60DD933EB8A44DC51CC536BDF3692CE5121146B`。
- `apps/web/src/assets/mysimple-logo.png`
  - 从冻结快照复制；SHA-256 为 `1AD04589B6072733667BFB0A1D8C6B0B4DCF45236B92373E9CFFF4666B319D15`。

### 测试代码

- `apps/web/src/App.test.tsx`
  - 覆盖个人主页、联系方式文本、侧栏折叠、返回工作台和主题按钮。
  - 既有功能测试在默认个人主页下显式进入工作台后继续验证正式行为。
- `apps/web/e2e/desktop-workspace.spec.ts`
  - 覆盖默认个人主页、进入工作台、主题/配置、任务详情及输出预览。
  - 新增侧栏展开与折叠截图，并验证折叠可逆。

## 3. 已回迁的视觉内容

- 主色 `#FF5B04`、暖灰边界 `#E7E5E5`、浅色画布 `#FAFAFA` 及对应暗色 Token。
- 系统字体栈、等宽数据字体、字号层级、圆角、阴影和留白节奏。
- 348 px 展开侧栏和 84 px 折叠侧栏，以及明确的导航激活指示。
- 半透明侧栏和顶栏、安静的径向画布、按钮即时按压反馈。
- Mysimple 场景型个人主页、玻璃信息卡、Logo、联系文本和工作台入口。
- 正式主题切换入口；主题继续由现有偏好状态持久化。
- 对减少动态、减少透明度、高对比度和键盘焦点的基础支持。

设计校准采用了 Apple 式的克制层级、可中断且不过度弹跳的折叠反馈，以及更有辨识度的个人场景主页；这些只影响表现和交互反馈，没有扩展产品能力。

## 4. 未回迁的静态演示内容

以下内容没有进入正式功能：

- 第三阶段 HTML 中模拟的 Worker、模型、IPC、环境、任务数量和性能状态。
- 模拟媒体、队列、转录进度、任务历史、固定日志、输出路径和 Toast 数据。
- 原型中的固定 Preset 参数、Beam 默认值、格式默认值和输出目录。
- 原型的硬件详情、计时语义、哈希路由、浏览器产品入口和演示用 `localStorage` 状态。
- HTML 构建脚本、SVG sprite 和整页脚本的机械复制。
- 工作台输入/Preset/参数/输出布局；该范围属于批次 2。
- 任务、性能和设置页面的视觉重构；该范围属于批次 3。
- 更完整的状态反馈、帮助、无障碍和响应式审计；该范围属于批次 4。

侧栏折叠偏好是独立的纯 UI 状态，使用专用键 `whisper-subtitle.sidebar-collapsed.v1`；它不代表 Worker、任务或 Desktop IPC 状态。

## 5. 正式功能和契约保护结论

- Python Worker 与转录算法：未修改。
- faster-whisper/CTranslate2 与 CUDA：未修改。
- Desktop IPC v1 schema、命令、事件和生命周期：未修改。
- Preset ID、真实参数值和语义：未修改。
- 后处理、文件命名、输出路径和输出内容：未修改。
- 任务状态、历史、取消、重试和参数快照：未修改。
- Tauri、WebView2、本地 EXE、CSP、能力白名单和零端口架构：未修改。
- 侧栏宿主、Worker、模型、环境和任务信息仍来自正式状态，没有写入原型模拟值。
- 主题切换继续调用正式 `setTheme`，没有建立第二套产品主题状态。

## 6. 验收与验证结果

实际执行并通过：

- `corepack pnpm typecheck`：通过。
- `corepack pnpm lint`：通过。
- `corepack pnpm test`：4 个测试文件、9 个测试通过。
- `corepack pnpm e2e`：先完成生产 Vite 构建，再由 Playwright 以 1 个 worker 运行 4 个场景，4/4 通过，用时约 9.4 秒。
- E2E 构建产物包含个人主页背景、Logo、CSS 和正式应用 JavaScript；构建成功。
- 展开侧栏截图像素检查：边界为 348 px。
- 折叠侧栏截图像素检查：边界为 84 px。
- 人工查看个人主页展开/折叠截图和浅色工作台截图：通过本批次视觉范围验收。

本批次没有执行且不声称完成：

- Python/Vitest 以外的 Worker、golden 或 CUDA 验收。
- Rust 单元/集成测试。
- Tauri、PyInstaller、NSIS、便携包或 Windows EXE 构建。
- WebView2 中的真实 Worker、GPU 和 EXE 桌面验收。

这些项目保留到批次 5，不以浏览器 E2E 代替正式桌面验收。

## 7. Git 与既有本地修改保护

执行期间正式工程保持：

- 分支：`master`。
- HEAD：`bf1313afe7860660d367d71f9459b81dc4f461f9`。
- 没有 staged 变化。
- 没有暂存、提交、创建分支、标签、推送或发布。

本批次新增或修改的范围为：

- `apps/web/src/App.tsx`
- `apps/web/src/components/Sidebar.tsx`
- `apps/web/src/components/HelpTip.tsx`
- `apps/web/src/components/PersonalHome.tsx`
- `apps/web/src/state/workspace.ts`
- `apps/web/src/state/persistence.ts`
- `apps/web/src/styles.css`
- `apps/web/index.html`
- 两个 Mysimple 图像资产
- `apps/web/src/App.test.tsx`
- `apps/web/e2e/desktop-workspace.spec.ts`
- 本第四阶段索引与批次 1 执行报告

保护结果：

- `.claude/settings.local.json` 未被本批次修改、删除、移动、暂存或提交。
- `.workbuddy` 未被本批次修改、删除、移动、暂存或提交。
- 既有 `Log/README.md` 和 `Log/30-第三阶段-独立UI设计沙盒/README.md` 变化未被触碰。
- 批次前已修改的 `OutputPanel.tsx`、`PerformanceStrip.tsx`、`PresetPanel.tsx`、`SettingsView.tsx`、`SourcePanel.tsx` 和 `TaskList.tsx` 哈希与批次 0 保护基线一致，未被本批次改写。
- `C:\Users\Administrator\Desktop\WhisperUI` 未修改；冻结快照关键文件哈希保持不变。
- 没有恢复 `.planning`，没有删除文件。

## 8. 遗留问题与风险

- 工作台内容区仍是正式工程批次前布局，与新外壳之间的最终视觉对齐属于批次 2。
- 任务、性能和设置页面尚未迁移第三阶段视觉，属于批次 3。
- 本批次只完成浏览器层面截图和交互验证；真实 Tauri/WebView2/Worker 验收仍需批次 5。
- 个人主页背景原图约 3.14 MB，已在前端生产构建中通过；是否进一步优化体积应作为明确后续批次决策，不能在本批次顺手处理。
- 第三阶段后续变化不会自动同步；本实现继续绑定 `UI-2026-07-17-R01`。

## 9. 批次 1 验收标准结论

- 使用已冻结快照，未自动采用第三阶段后续变化：通过。
- 全局 Token、应用外壳、侧栏、顶栏、折叠、主题和个人主页完成正式 React 映射：通过。
- 原型模拟数据未进入正式状态：通过。
- 正式宿主状态、主题状态和工作台路由继续使用现有实现：通过。
- Python、IPC、Preset、任务与输出契约保持不变：通过。
- 前端类型检查、Lint、单元测试、生产构建和 E2E 通过：通过。
- 第三阶段工作区和既有本地修改受到保护：通过。
- 未执行未经授权的 Git 操作：通过。

## 10. 下一步门禁

批次 1 完成后停止。只有用户明确回复“执行第四阶段批次2”后，才能开始转录工作台、输入、Preset、参数、输出、真实媒体队列和启动卡的正式回迁；不得自动跨批次执行。
