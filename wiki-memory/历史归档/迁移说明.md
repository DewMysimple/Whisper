# 历史归档

本目录保存迁移前 `F:\WhisperSubtitle\Log` 的完整内容，原有阶段层级和文件正文不做批量改写。它只用于审计、追溯原始需求和查找历史执行证据，不代表当前实现或默认推荐方案。

## 迁移映射

```text
F:\WhisperSubtitle\Log\  →  F:\WhisperSubtitle\wiki-memory\历史归档\
```

历史中出现的 `Log/` 路径是当时记录的路径；当前有效路径使用 `wiki-memory/`。当前事实从 `wiki-memory/当前状态/`、项目根 `README.md` 和 `docs/` 开始读取。

## 归档内容

- `00-项目记忆/`：旧的状态、决策和交接资料。
- `10-第一阶段-基础修复与模块化重构/`：基础修复与模块化过程。
- `20-第二阶段-Tauri桌面架构迁移/`：桌面架构迁移过程。
- `30-第三阶段-独立UI设计沙盒/`：UI 设计沙盒索引。
- `40-第四阶段-UI成果回迁与正式应用/`：正式 UI、功能测试和错误处理证据。
- `80-历史评估/`：重构前评估。
- `90-原始需求/`：用户原始需求，内容只读保留。
