---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-08-23
topic: project-memory-rebuild
source_logs: []
supersedes: null
---

# WhisperSubtitle 项目记忆重建

## 目标

将旧 `Log` 重命名为 `wiki-memory`，保留完整历史，同时建立精简的当前记忆入口，并接入项目根目录 Agent 读取协议。

## 已完成

- 旧 `Log` 已迁移到 `wiki-memory/历史归档/`。
- 原有历史文件共 200 个，总字节数为 1,738,918；旧 `Log/README.md` 已单独恢复为归档 `README.md`，迁移说明另存为 `迁移说明.md`。
- 旧阶段目录和原始需求保留；没有补造缺失的 `isolate.md`。
- 新建当前状态、决策、知识、日志、模板和工具入口。
- 新增项目根目录 `AGENTS.md` 轻量指针。
- 未迁移模板中的 `llm-wiki.md` 和 `__pycache__`。

## 当前工作树说明

迁移前发现当前 Git 工作树已有未提交的 React/Tauri UI 修改，以及 `UI-2026-07-28-R03` 相关文档。它们没有被记忆重建覆盖、回滚或伪装成已封版事实。

## 事实来源

当前状态来自 `README.md`、`docs/`、`src/`、`apps/`、`contracts/`、`packaging/`、`tests/` 和现有历史索引。历史执行报告仅保留在 `历史归档/`，不参与默认读取。

## 验证记录

- `Log` 源目录不存在，`wiki-memory` 存在；归档 200 个历史文件、1,738,918 字节，旧 README Git blob 哈希一致。
- `python wiki-memory/工具/memory_lint.py check` 通过，检查 28 个 Markdown 页面。
- 有效范围内没有残留 `Log/` 路径引用；26 个 artifact/架构测试通过。
- 源码路径下完整 pytest（排除依赖 `psutil` 的 benchmark 收集项）为 309 通过、37 失败；失败集中在当前环境缺少 `opencc`。
- `python -m whisper_subtitle check` 已执行，但当前环境缺少 `psutil`、`faster_whisper`、`ctranslate2`、`opencc` 和可发现模型，因此未通过环境检查；未安装依赖或下载模型。
- 未执行 Tauri 构建、CUDA 重测、UI 行为修改或 Git 提交。

## 待确认长期记忆

- 当前未提交 UI 修改何时完成验收并成为新的正式状态。
- 第二套真实模型完整验收、系统通知点击复验和复杂 code-switch 质量边界的后续结论。
