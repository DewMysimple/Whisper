---
type: log
status: active
kind: maintenance
importance: normal
updated: 2026-08-24
topic: repository-agent-state-cleanup
source_logs: []
---

# 清理本地 Agent 工具状态

## 目标

确认并清理仓库根目录中不参与 WhisperSubtitle 运行、构建、测试或打包的 `.agents/`、`.claude/` 和 `.workbuddy/`，并补充面向 GitHub 访客的项目 README。

## 检查结论

- `.agents/` 为空。
- `.claude/` 只有本地权限配置 `settings.local.json`，没有源码、构建或测试引用。
- `.workbuddy/` 只有旧工具记忆和临时脚本，应用源码、脚本、测试和打包流程均不读取它。
- 活跃文档曾要求保留 `.workbuddy/`；该规则已更新为本地 Agent/工具状态不属于产品边界，不需要时可清理。
- 历史归档中的旧执行记录继续保留，作为历史事实，不代表当前目录策略。

## 变更

- 删除 `.agents/`、`.claude/`、`.workbuddy/`。
- 删除 `.prettierignore` 中已经无效的 `.workbuddy` 忽略项。
- 重写 `README.md`，补充项目定位、功能、四种 preset、架构、安装、CLI、桌面开发、测试、Windows 打包、目录结构和中英文说明。
- 更新 `docs/repository_policy.md` 和 `wiki-memory/当前状态/当前约束.md`。

## 验证

- 使用 Git 跟踪文件和非历史文档搜索目录引用。
- 删除后确认三个目录不存在，应用代码引用为零。
- 运行项目测试/检查和记忆体检后提交本次清理。
