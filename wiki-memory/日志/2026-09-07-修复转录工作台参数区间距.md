---
type: log
status: active
kind: ui
importance: medium
updated: 2026-09-07
topic: transcription-workspace-spacing
source_logs: []
---

# 修复转录工作台参数区间距

## 目标

根据用户提供的截图，调整“文本识别模式”中第二行模式卡片与下方模型参数入口之间过近、且与模式卡片行间距不一致的问题。

## 授权与范围

- 用户明确授权本次正式 UI 修改，作为当前约定的第 32 次 UI 修改记录。
- 视觉差异依据用户截图；页面结构和样式事实依据当前正式 `apps/web` 源码。
- 仅修改转录工作台的 CSS 间距；未修改 Python、IPC v1、Preset、模型、输出规则或任务状态。

## 变更

- `apps/web/src/styles.css`：将 `.preset-parameter-link` 的 `margin-top` 从 `0` 调整为 `var(--space-3)`，与上方模式卡片的 12px 行间距保持一致。

## 验证

- `corepack pnpm check:preset-catalog`：通过。
- `corepack pnpm format:check`：通过。
- `corepack pnpm lint`：通过。
- `corepack pnpm typecheck`：通过。
- `corepack pnpm test`：20 个测试文件、87 个测试通过。
- `corepack pnpm --filter @whisper-subtitle/web e2e`：11 个 Playwright 场景通过。
- `corepack pnpm desktop:build`：Tauri Release 构建通过，生成 `apps/desktop/src-tauri/target/release/whisper-subtitle-desktop.exe`。
- 新 EXE 启动烟测：进程启动后保持运行 5 秒，未异常退出，测试进程已关闭。

## 遗留问题

- 构建仍报告既有的 JavaScript chunk 大小提示；与本次 CSS 间距修改无关。
- 未更新 active 状态页；本次变更不改变长期架构、约束或入口事实。

## 待确认记忆

- 暂无需要提升到 active 页面或决策的长期结论。
