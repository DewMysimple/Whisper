---
type: log
status: archived
kind: ui
importance: medium
updated: 2026-09-08
topic: srt-parameter-unit-alignment
source_logs:
  - "[[日志/2026-09-08-删除界面问号与提示功能]]"
---

# 第 33 次修改（第 2 项）：SRT 参数单位右对齐

## 目标与授权

用户要求修正 SRT 排版参数中单位文字过于靠近左侧标签、且与上方模式卡片间距观感不一致的问题。实际布局依据正式 SRT 参数组件和浏览器几何结果调整。

## 变更

- `apps/web/src/components/ProfileParameterEditor.tsx`：将 SRT 参数标签和单位拆分为可独立布局的元素。
- `apps/web/src/styles.css`：使用 flex 布局将单位推到每个字段行的最右侧，保留字段标签的截断空间。
- `apps/web/e2e/desktop-workspace.spec.ts`：增加实际 DOM 几何断言，验证所有 SRT 单位右边缘与标签行右边缘对齐。

本项独立于已推送的“删除问号与提示功能”，未修改输出格式、任务契约、Python Worker 或 IPC v1。

## 验证

- `corepack pnpm check`：通过；20 个测试文件、87 个测试通过。
- `corepack pnpm --filter @whisper-subtitle/web e2e`：11 个 Playwright 场景通过。
- `corepack pnpm desktop:build`：Tauri Release `--no-bundle` 构建通过。
- 正式 EXE 启动烟测：启动 5 秒后仍存活，主窗口句柄有效，测试进程已关闭。
- `python wiki-memory/工具/memory_lint.py index` 与 `check`：通过。

## 提交与后续

- 本项独立创建 Git 提交并推送到当前分支远程。
- 下一项按顺序处理“生成文件格式”图标化。
