---
type: log
status: archived
kind: ui
importance: high
updated: 2026-09-08
topic: remove-ui-help-prompts
source_logs:
  - "[[日志/2026-09-07-修复SRT切换碎片与参数入口]]"
---

# 第 33 次修改（第 1 项）：删除界面问号与提示功能

## 目标与授权

用户明确要求删除转录工作台及相关页面中的问号和提示功能，并保留必要的安全确认对话框。截图中的视觉标注仅用于定位；实际改动依据正式 React/Tauri 工程源码。

## 变更

- 删除 `HelpTip` 组件、问号触发器、悬浮说明和对应样式。
- 删除非安全操作上的原生 `title` 提示与任务列表自定义 tooltip。
- 保留覆盖文件、跳过同名媒体、终止任务、关机等安全确认流程；去除对话框标题中的问号标点。
- 将输出面板中的疑问式说明改为陈述式文案。
- 更新组件测试和桌面 E2E，增加页面不再出现帮助/tooltip/title 的回归断言。

本项未包含 SRT 参数单位对齐、生成格式图标化或 SRT/TXT 独立选择；这些按用户要求作为后续独立修改提交。

## 验证

- `corepack pnpm check`：通过；20 个测试文件、87 个测试通过。
- `corepack pnpm --filter @whisper-subtitle/web e2e`：11 个 Playwright 场景通过。
- `corepack pnpm desktop:build`：Tauri Release `--no-bundle` 构建通过。
- 正式 EXE 启动烟测：启动 5 秒后仍存活，主窗口句柄有效，测试进程已关闭。
- `python wiki-memory/工具/memory_lint.py index` 与 `check`：通过。

## 提交与后续

- 本项独立创建 Git 提交并推送到当前分支远程；以 Git 远程核对结果为准。
- 下一项为 SRT 参数单位右对齐，必须在本项提交推送完成后开始。
