---
type: log
status: archived
kind: ui
importance: high
updated: 2026-09-08
topic: output-format-icon-controls
source_logs:
  - "[[日志/2026-09-08-SRT参数单位右对齐]]"
---

# 第 33 次修改（第 3 项）：生成文件格式图标化

## 目标与授权

用户要求“生成文件格式”区域去除外层 UI 框和“纯文本 / 直接存入 Text 文件夹”等说明，只保留 TXT、MD 图标，并以橙色高亮表达选中状态。

## 变更

- `apps/web/src/components/OutputPanel.tsx`：转录模式的 TXT、MD 选项改为紧凑的 56px 图标控件；移除格式说明文案，保留原有 checkbox 状态与输出逻辑。
- `apps/web/src/styles.css`：将格式选项改为横向图标组，去除卡片边框和说明文本样式；选中图标使用橙色柔和背景与边框，并保留键盘焦点可见性。
- `apps/web/src/App.test.tsx`、`apps/web/e2e/desktop-workspace.spec.ts`：改用格式 checkbox 的可访问名称交互，并增加无说明节点、双图标和 TXT 选中态断言。

本项只处理转录模式的视觉和交互表达，不改变输出状态字段、目录规则或 SRT 模式；SRT/TXT 独立生成将在下一项单独处理。

## 验证

- `corepack pnpm check`：通过；20 个测试文件、87 个测试通过。
- `corepack pnpm --filter @whisper-subtitle/web e2e`：11 个 Playwright 场景通过。
- `corepack pnpm desktop:build`：Tauri Release `--no-bundle` 构建通过。
- 正式 EXE 启动烟测：启动 5 秒后仍存活，主窗口句柄有效，测试进程已关闭。
- `python wiki-memory/工具/memory_lint.py index` 与 `check`：通过。

## 提交与后续

- 本项独立创建 Git 提交并推送到当前分支远程。
- 下一项按顺序处理 SRT 模式下 SRT 与 TXT 的独立生成选择。
