---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-09-09
topic: periodic-engineering-maintenance
source_logs: []
supersedes: null
---

# 工程定期维护

## 目标

评估并维护当前工程的架构、工程文件、目录管理、Agent 入口、README 和长期记忆，在不改变产品功能契约的前提下修复本期应处理的问题。

## 本期维护

- 将模型身份、能力与仓库映射统一到 `domain/models.py`，生成 TypeScript/Rust 投影，并校验 Desktop IPC schema 中的模型枚举。
- 将 Worker 媒体路径归一化、目录展开和时长探测从总运行时移入 `worker/media.py`。
- 将系统提醒和电源倒计时通知纳入 typed `DesktopBridge`，用 eslint 阻止 bridge 外直接导入 Tauri API。
- 增加分层架构测试、仓库卫生检查和记忆维护测试；Python 依赖以 `pyproject.toml` 为唯一事实源。
- 停止跟踪机器相关 `Whisper.lnk`，但保留本地文件；完善 Agent 入口、README、维护指南与长期记忆协议。

## 继续保持

- Desktop IPC v1、四个 Preset、输出路径/内容、现有 UI 行为和正式桌面入口不变。
- `workspace.ts`、Worker runtime 和 Rust Host 仅按清晰责任边界拆分，不因行数继续碎片化。
- 模型、虚拟环境、构建缓存、Rust target、发布产物和用户输出均保留。

## 延后事项

- 协议级 schema 代码生成只在后续需要扩展 Desktop IPC 时单独设计。
- 真实第二模型验收、系统通知点击返回、复杂 code-switch 质量仍需独立资源或用户验收。
- 大型本地构建缓存只作为可选清理机会，未获授权不删除。

## 验证

- `python scripts/generate_model_catalog.py --check`、Preset 投影检查、仓库卫生检查和记忆体检通过。
- Python 全套测试：363 项通过。
- Web 格式、eslint、TypeScript 与 Vitest：20 个测试文件、88 项测试通过。
- Playwright：12 个桌面工作台场景通过。
- Rust `cargo fmt --check`、`cargo test --locked`、clippy 通过；33 项测试通过，2 项真实声音/GPU 测试按设计忽略。
- 在仓库本地模型目录下运行环境检查通过。
- Tauri Release 构建通过；生成的桌面 EXE 保持运行 5 秒，烟测进程随后关闭。
- 构建仍有既有的 JavaScript chunk 大小提示和 MSVC linker 信息提示，不影响本期验收。
