---
title: 对齐历史事实区文字
type: log
status: archived
kind: ui
updated: 2026-09-19
importance: medium
topic: align-history-fact-text
source_logs:
  - "[[日志/2026-09-19-收紧历史卡片标题与分隔线]]"
supersedes: null
---

# 对齐历史事实区文字

## 目标

- 修正历史任务卡中配置行与统计行虽然列线同轴、但第二和第三列文字起点不一致的问题。

## 实际变更

- 将配置行和统计行的三列内容统一为 8px 内缩；首列继续贴合内容面左边缘，末列继续贴合右边缘。
- 保留原有三等分列线、文字层级、主题配色和任务信息，不改变任何任务行为。
- Playwright 几何断言新增逐列文字左边缘校验，除列线之外同步约束两排文字对齐。

## 验证

- 定向 Playwright 场景通过，截图确认“转录模式／文本格式／参数配置”与“媒体／耗时／媒体时长”逐列对齐。
- `corepack pnpm check` 通过：仓库卫生、记忆体检、代码生成检查、格式、ESLint、TypeScript 与 24 个测试文件共 98 项测试全部通过。
- 完整 Playwright E2E 15 项通过。
- `corepack pnpm desktop:build` 通过；Release EXE 启动后窗口可响应，标题为 `WhisperSubtitle`，并观察到受 Host 监管的 Worker 与 WebView2 子进程。烟测结束后已清理本轮进程。

## 保留项

- 历史卡片的字段、列宽、列线、配色、图标、任务操作和响应式列数保持不变。
