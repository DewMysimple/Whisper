---
title: 重建并同步最新便携版
type: log
status: archived
kind: operations
updated: 2026-09-19
importance: high
topic: rebuild-latest-portable-release
source_logs: []
---

# 重建并同步最新便携版

## 目标

从当前已同步的 `master` 源码重新构建完整便携版，并整体替换 `dist/WhisperSubtitle/`、`dist/WhisperSubtitle.zip` 与外层校验文件，确保用户拿到最新桌面、冻结 Worker、模型布局和发布元数据。

## 实际结果

- 以源码提交 `f830ac1` 为基线运行标准 `corepack pnpm release:build`，从全新 `build/release/` 环境重新安装固定发布依赖、构建工程 wheel、冻结 Worker、编译 Web 与 Tauri Release Host。
- 发布脚本整体重建 `dist/WhisperSubtitle/`，同时生成同内容 `dist/WhisperSubtitle.zip` 与 `dist/WhisperSubtitle.sha256`；没有生成本轮未要求的 NSIS 安装介质。
- 成品版本为 `0.1.0`。manifest 覆盖除自身外的 930 个文件，ZIP 包含与便携目录一致的 931 个文件。
- ZIP 大小约 2.304 GiB，SHA-256 为 `d7a82caacbe7923be875c1e74beeec9a406d1d170786ab9f75b6ab10d0e204c2`。

## 验证

- `corepack pnpm check`：通过；仓库卫生、记忆、模型与 Preset 投影、样式所有权、格式、ESLint、TypeScript 和 32 个测试文件中的 127 项测试均通过。
- 对 manifest 的 930 个文件逐项复算大小与 SHA-256：全部通过；没有缺失或额外文件。
- 对 `dist/WhisperSubtitle.sha256` 的 3 个关键条目重新计算 SHA-256：全部通过。
- 比较 ZIP 与便携目录的路径和未压缩大小：931 个文件完全一致。
- 从 `dist/WhisperSubtitle/WhisperSubtitle.exe` 启动并持续观察 40 秒：主窗口保持响应、标题为 `WhisperSubtitle`，冻结 Worker 始终从 `_internal/worker/whisper-subtitle-worker.exe` 运行且未重启，越过 Host 的 30 秒 ready 超时门槛；WebView2 正常创建，退出后无残留进程。

## 结论

`dist/` 中的完整便携目录、ZIP 与校验文件已经同步到当前最新产品代码，可从 `dist/WhisperSubtitle/WhisperSubtitle.exe` 直接启动；交付时必须保留整个 `WhisperSubtitle` 目录，不能单独复制 EXE。
