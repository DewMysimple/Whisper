---
title: 重建并验收最新便携版
type: log
status: archived
kind: operations
updated: 2026-09-23
importance: high
topic: portable-release-rebuild-2026-09-23
source_logs: []
---

# 重建并验收最新便携版

## 目标与范围

用户在完成 2026-09-23 的开发态 UI 整改后，明确授权构建新应用。本次使用默认便携版发布入口同步重建正式 Web、Tauri Host、冻结 Worker、离线模型副本、SBOM、manifest、ZIP 与外层校验文件；未生成可选 NSIS 安装器，也未改变版本号、IPC、模型或识别输出规则。

## 构建结果

- `corepack pnpm release:build` 成功，正式入口更新为 `dist/WhisperSubtitle/WhisperSubtitle.exe`，同内容压缩包为 `dist/WhisperSubtitle.zip`。
- ZIP 大小为 2,473,973,433 字节，SHA256 为 `6ece9cc55d8e6a509ff6e751eeb3a74427ed04adebe0a6e52ad3527097049445`。
- EXE 大小为 11,321,856 字节，SHA256 为 `119462f9f811561e310faa677acf7c6d894743f9c4b173926ac2d0796b26ce93`。
- manifest 包含 930 个受管条目；便携目录共有 931 个文件，额外一项为 manifest 本身。ZIP 的 931 个路径与大小和目录一致，全量 CRC 读取通过；manifest 中每个文件的大小和 SHA256、外层三项 SHA256 均独立复核通过。

## 运行验收

- 冻结 Worker 独立执行 `worker.ready → system.health → worker.shutdown`，健康状态为 `ok`，退出码为 0，stderr 为空。
- 从正式 `dist` 启动真实 EXE，内嵌页面地址为 `http://tauri.localhost/`；Host 达到 `ready`，`launchKind` 为 `packaged-sidecar`，其 PID 与 `dist/WhisperSubtitle/_internal/worker/whisper-subtitle-worker.exe` 完全对应。
- 同一 Worker 持续就绪 15 秒且只出现一次 ready 事件；通过原生窗口关闭后，桌面进程、Worker 和临时调试端口全部退出。
- `corepack pnpm check` 通过：仓库卫生、91 页记忆体检、模型／preset 投影、12 项样式门禁、格式、lint、类型检查与 Web 单测 149 项全部通过。
- 本轮源码只包含此前已通过 Web、浏览器与 Rust 门禁的 UI／目录入口整改，因此发布验收聚焦打包完整性、冻结 Worker 协议和真实 EXE 生命周期；没有重复执行 GPU 转录或输出 golden。
