---
type: knowledge
status: active
kind: operations
importance: high
updated: 2026-09-20
topic: release-and-model-management
source_logs:
  - "[[日志/2026-09-20-硬件优化与工作台统一及便携版更新]]"
  - "[[日志/2026-08-23-项目记忆重建]]"
  - "[[日志/2026-09-17-优化仓库与发布目录结构]]"
  - "[[日志/2026-09-19-重建并同步最新便携版]]"
supersedes: null
---

# 发布与模型管理

- `tools/release/build.ps1` 构建冻结 Worker、便携目录、ZIP、manifest、SHA-256 和 SBOM；`corepack pnpm release:build` 是默认便携版入口，NSIS 只由 `release:build:installer` 显式生成。
- 发布运行时不依赖 Python、Node.js 或 Rust；Worker、CUDA 用户态依赖、WebView2 要按发布布局验证。
- 模型使用 `_internal/models/large-v3-turbo/` 的直接离线快照；发布脚本负责版本一致性和产物清单。
- 正式交付只认 `dist/WhisperSubtitle/` 与 `dist/WhisperSubtitle.zip`；技术依赖进入 `_internal/`。`target/release` EXE 只用于开发编译，不是完整交付物。
- 可选安装介质隔离在 `dist/installer/`，setup 必须与完整 `_internal/` 相邻；模型位于 `_internal/models`，微软签名的 WebView2 x64 离线安装器位于 `_internal/distribution`。bundler 不下载前置依赖。

2026-09-20 已按用户授权将模型／推理参数和硬件执行设置配套构建进入 dist；完整清单、ZIP／校验和、冻结 Worker 与正式 EXE 转录验收通过，证据见关联任务日志。不能混用旧冻结 Worker 与新前端／Host。便携版中文说明源为 `tools/release/user_guide.zh-CN.md`，应在 assemble 之前更新，不能仅修改已生成 dist 内的副本。

当冻结 Worker 和 stage 未变、仅修正 Web 或 Host 时，可直接运行 `tools/release/assemble.ps1`；它仍重新构建 Web／Tauri，再整体生成便携目录、manifest、ZIP 和 SHA256。本轮错误文案修复即按该流程重新组装并完整复验，不能只替换成品 EXE。
