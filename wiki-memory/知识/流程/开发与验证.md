---
type: knowledge
status: active
kind: process
importance: high
updated: 2026-09-19
topic: development-validation
source_logs:
  - "[[日志/2026-09-19-任务监控与记录深度维护]]"
  - "[[日志/2026-09-19-开发态功能与UI可维护性整改]]"
  - "[[日志/2026-08-23-项目记忆重建]]"
  - "[[日志/2026-08-24-架构瘦身实施]]"
  - "[[日志/2026-08-24-架构瘦身收口]]"
  - "[[日志/2026-09-07-建立修改后提交推送约定]]"
  - "[[日志/2026-09-09-工程定期维护]]"
  - "[[日志/2026-09-17-修复桌面IPC启动失败]]"
  - "[[日志/2026-09-17-优化仓库与发布目录结构]]"
  - "[[日志/2026-09-17-启用Vite开发服务器并审计Apps目录]]"
supersedes: null
---

# 开发与验证

常规验证：

```powershell
python tools/codegen/generate_model_catalog.py --check
python tools/codegen/generate_preset_catalog.py --check
python tools/maintenance/check_repository_hygiene.py
corepack pnpm check
corepack pnpm check:styles
.\whisper_env\Scripts\python.exe -m pytest -q
python wiki-memory/工具/memory_lint.py index
python wiki-memory/工具/memory_lint.py check
```

UI 开发入口：`corepack pnpm web:dev` 提供浏览器/mock bridge 预览，`corepack pnpm desktop:dev` 由 Tauri 自动启动同一个 `127.0.0.1:1420` Vite/HMR 服务并连接真实 Worker。正式构建仍使用静态 `apps/web/dist/`，不依赖开发服务器。

系统 Python 未安装项目依赖时，应选择当前已安装工程的虚拟环境解释器，不把纯导入失败当作代码回归。涉及 UI 时补充 `corepack pnpm e2e`；涉及 Rust Host 时运行 fmt、test 和 clippy；涉及桌面产物时补充 Tauri Release 构建和启动烟测。桌面烟测必须确认受控 Worker 子进程持续运行并已通过握手，不能只检查桌面 EXE 未退出。涉及 GPU、依赖、模型或输出行为时，必须增加真实媒体、四 preset、冻结 Worker 和必要的 benchmark/golden 验证。

## 修改后的提交与推送

每完成一次仓库修改任务并通过对应验证，必须立即创建独立 Git 提交并推送到当前分支的远程。推送失败时如实报告原因，保留本地提交，不使用强推或历史改写；提交前确认范围外的用户改动未被混入。

Golden 只能在明确接受行为变化后更新；benchmark 至少区分冷进程和暖系统缓存。测试结果必须写入对应任务日志，不凭计划补写结果。

`check:styles` 已纳入 `check`，先运行检查器的 5 项测试，再由 PostCSS 递归检查 Web 源码 CSS。任务样式强制按责任文件归属，同条件下不得重复选择器（包括选择器列表中的单项）或属性，不得使用 `!important` 和旧页面祖先变体；其他旧 CSS 继续使用保守的同规则属性覆盖检查，`--fix` 只清理可确定被覆盖的旧声明，不修正归属问题。整理级联时应先保留旧样式，再在相同 DOM 上比较修改前后的计算属性，并覆盖浅色／深色与支持的窗口宽度；不能仅凭构建成功判断样式等价。
